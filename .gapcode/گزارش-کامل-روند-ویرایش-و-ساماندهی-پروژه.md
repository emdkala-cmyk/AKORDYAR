# گزارش کامل روند ویرایش و ساماندهی پروژهٔ Akordyar

> آخرین به‌روزرسانی: ۱۹ مرداد ۱۴۰۵ (۱۰ اوت ۲۰۲۶)
> آخرین کامیت فنی: a3ae90a

---

## هدف کلی

Refactor تدریجی پروژهٔ Akordyar با استخراج منطق از js/app.js به ماژول‌های مستقل در js/core/،
همراه با characterization test و حفظ کامل رفتار موجود.

---

## ماژول‌های استخراج‌شده

| # | ماژول | فایل | commit | وضعیت |
|---|---|---|---|---|
| ۱ | SongMetadata | js/core/SongMetadata.js | 057cfa3 | ✅ کامل |
| ۲ | IconRegistry | js/core/IconRegistry.js | ec02d1a | ✅ کامل |
| ۳ | TransposeService | js/core/TransposeService.js | 89eac72 | ✅ کامل |
| ۴ | TimelineGrid | js/core/TimelineGrid.js | 61f8627 | ✅ کامل (محاسبات به Meter واگذار شد) |
| ۵ | Meter | js/core/Meter.js | 5aa0097 | ✅ کامل — منطق خالص متر/ضرب |
| ۶ | PlayheadMath | js/core/PlayheadMath.js | c3ef060 | ✅ کامل — محاسبات خالص playhead |
| ۷ | EventBindings | js/core/EventBindings.js | b8935ce، 7f76ada | ✅ کامل — ثبت متمرکز رویدادها و مهاجرت transport/navigation/quick search |
| ۸ | MetronomeEngine | js/core/MetronomeEngine.js | a3ae90a | 🟡 Safe Extraction — محاسبه beat transition و accent با bridge سازگار |
| ۹ | AudioContextService | js/core/AudioContextService.js | — | 🟡 Safe Extraction — مدیریت Web Audio و سنتز صدای کلیک مترونوم |
| ۱۰ | MetronomeScheduler | js/core/MetronomeScheduler.js | — | 🟢 Look-ahead Scheduler — رزرو ضربات در audioCtx.currentTime (الگوی Chris Wilson) |

---

## ماژول‌های باقی‌مانده (هنوز استخراج نشده)

- [ ] ArchiveService
- [ ] Clipboard/Paste logic
- [ ] Lyric parser integration
- [ ] UI handlers (ادامهٔ مهاجرت گروه‌های باقی‌مانده)

---

## باگ‌های رفع‌شده

| # | شرح | commit |
|---|---|---|
| ۱ | اعمال بمل/دیز بر اساس گام انتخاب‌شده + slash chord | ec642f2 |
| ۲ | همگام‌سازی baseChordNames پس از ویرایش دستی آکورد | acbf114 |
| ۳ | ReferenceError: timeline is not defined در renderRuler (regression استخراج TimelineGrid) | 26e5adc, 590f93b |

---

## تغییرات UI

| # | شرح | commit |
|---|---|---|
| ۱ | تبدیل ۱۱ دکمهٔ close/clear از emoji ✕ به SVG | 34dc809 |

---

## آمار کلی

- **کامیت‌ها**: ۱۵
- **فایل جدید core**: ۸
- **خط حذف از app.js**: ~۲۹۳
- **خط اضافه (ماژول جدید + تست)**: ~۲,۰۵۰
- **تست نهایی**: 135 passed, 0 failed

---

## تاریخچهٔ کامل کامیت‌ها

| commit | پیام | فایل‌ها |
|---|---|---

---

## فاز جاری: تحلیل Playback/Transport

**وضعیت:** تحلیل وابستگی‌ها انجام شد — به‌جای استخراج کامل PlaybackManager، ماژول PlayheadMath برای محاسبات خالص playhead ساخته شد.

### یافته‌های کلیدی

- حلقه‌ی tick در startTransport() حدود ۱۳۰ خط است و مستقیماً با DOM، DAW.state، Arranger و مترونوم درگیر است.
- DAW.isPlaying در +۳۰ نقطه از app.js به‌عنوان شرط guard استفاده می‌شود.
- جداسازی کامل نیازمند refactor هم‌زمان DOM coupling و state management است.

### مسیر پیشنهادی

1. **استخراج محاسبات خالص playhead** (مشابه Meter) — کم‌ریسک، فوری
2. **رفع لکنت مترونوم** با AudioContext look-ahead scheduling — مستقل از این فاز
3. **استخراج کامل PlaybackManager** — پس از migrate شدن UI handlers و state management

### دلایل فنی

- تابع startTransport() تیک loop را با DOM manipulation ترکیب کرده است
- Arranger logic و count-in مستقیماً در tick loop قرار دارند
- جدا کردن این بخش‌ها بدون شکستن تست‌های موجود ممکن نیست
- بهتر است ابتدا DOM coupling را کاهش دهیم، سپس transport را استخراج کنیم|
| 057cfa3 | refactor: استخراج SongMetadata و رفع Mojibake فایل app.js | SongMetadata.js جدید، app.js، Akordyar.html، تست |
| ec02d1a | refactor: افزودن زیرساخت رجیستری آیکون‌های SVG | IconRegistry.js جدید، app.js، Akordyar.html |
| 34dc809 | refactor: تبدیل دکمه‌های بستن UI به آیکون SVG | Akordyar.html، styles/base.css |
| 89eac72 | refactor: استخراج TransposeService | TransposeService.js جدید، MusicTheory.js، app.js |
| ec642f2 | fix: اعمال بمل بر اساس گام انتخاب‌شده | app.js، TransposeService.js، تست |
| acbf114 | fix: همگام‌سازی مبنای آکورد پس از ویرایش دستی | app.js، تست |
| 61f8627 | refactor: استخراج TimelineGrid | TimelineGrid.js جدید، app.js، Akordyar.html، تست |
| 1243a27 | feat: افزودن getGridStructure به TimelineGrid | TimelineGrid.js، تست |
| 26e5adc | fix: رفع ReferenceError در wrapper رندر تایم‌لاین | app.js |
| 590f93b | fix: رفع خطای تکراری ReferenceError در renderRuler | app.js |
| f354ac9 | chore: همگام‌سازی فایل‌های pre-existing | performanceStore.js، songDocumentModel.js، preload.js، server.js |
| 611266c | chore: افزودن فایل‌های تکمیلی پروژه | DomainBridge.js، EventBindings، .gapcode/ و سایر فایل‌های untracked |
| c3ef060 | refactor: استخراج PlayheadMath | PlayheadMath.js جدید، app.js، Akordyar.html |
| a9b499d | fix: رفع لکنت مترونوم هنگام زوم/اسکرول تایم‌لاین | app.js |
| 5aa0097 | refactor: استخراج ماژول Meter | Meter.js جدید، TimelineGrid.js، app.js، Akordyar.html، مستندات |
| b8935ce | ریفکتور: انتقال مدیریت رویدادها به ماژول EventBindings | app.js، EventBindings.js، گزارش روند ویرایش |
| 7f76ada | ریفکتور: مهاجرت رویدادهای رابط کاربری به EventBindings | Akordyar.html، app.js، EventBindings.js، گزارش روند ویرایش |
| a3ae90a | ریفکتور: اتصال امن موتور مترونوم با bridge سازگار | MetronomeEngine.js، app.js، Akordyar.html، تست، گزارش روند ویرایش |

---

## فاز EventBindings

**وضعیت:** تکمیل شد.

### تغییرات انجام‌شده

- ساختار IIFE قبلی `EventBindings` به کلاس مستقل تبدیل شد.
- وابستگی‌های رفتاری از طریق constructor دریافت می‌شوند و ماژول دیگر مستقیماً به توابع global در `app.js` وابسته نیست.
- ثبت رویدادهای transport، navigation و دو handler اصلی keyboard در متد `init()` متمرکز شد.
- متد `destroy()` برای حذف listenerهای ثبت‌شده و جلوگیری از نشت رویدادها اضافه شد.
- handlerهای اصلی keyboard در `app.js` به توابع نام‌دار تبدیل و برای ثبت به `EventBindings` تزریق شدند.
- نمونه‌سازی `EventBindings` در `app.js` انجام می‌شود و هر دو ترتیب بارگذاری اسکریپت‌ها پشتیبانی می‌شود.
- از ثبت چندباره listenerها و اجرای دوگانه دکمهٔ play در event delegation جلوگیری شد.

### اعتبارسنجی

- `node --check js/app.js`
- `node --check js/core/EventBindings.js`
- `git diff --check -- js/app.js js/core/EventBindings.js`

---

## فاز مهاجرت inline handlerهای رابط کاربری

**وضعیت:** مرحلهٔ transport، navigation و quick search تکمیل شد.

در این مرحله ۲۸ inline event attribute شامل `onclick`، `oninput` و `onchange` از سه گروه کنترل‌های transport، آیتم‌های navigation و پنل quick search در `Akordyar.html` حذف شد. ساختار DOM و کلاس‌های CSS حفظ شدند و فقط markerهای `data-event-group`، `data-action` و `data-command` برای اتصال declarative افزوده شدند. متدهای `bindTransportControls()`، `bindNavItems()` و `bindQuickSearchPanel()` در `js/core/EventBindings.js` با event delegation روی container واقعی هر بخش تکمیل شدند و `ACTION_FUNCTIONS` در `js/app.js` بدون بازنویسی منطق توابع مقصد گسترش یافت. همچنین initialization تا آماده‌شدن کامل DOM محافظت شد تا پنل quick search که بعد از scriptها در HTML قرار دارد نیز به‌درستی bind شود. این تغییر در راستای مهاجرت تدریجی inline handlerها، کاهش وابستگی HTML به توابع global و حفظ رفتار و پایداری فعلی برنامه انجام شد.

### Inventory مهاجرت

- transport: هشت رویداد click
- navigation: دوازده رویداد click
- quick search: سه رویداد click، یک رویداد input و چهار رویداد change

### اعتبارسنجی

- syntax هر دو فایل JavaScript با `node --check`
- نبود inline handler در محدوده‌های هدف
- تطابق ۱۶ command و ۸ transport action با registry
- تست مستقل delegation برای transport، navigation، quick search و `destroy()`
- `git diff --check` برای سه فایل فنی

---

## فاز Safe Extraction AudioContextService

**وضعیت:** اتصال اولیه و bridge سازگار تکمیل شد؛ استخراج کامل هنوز انجام نشده است.

فایل `js/core/AudioContextService.js` ایجاد شد تا مدیریت Web Audio و سنتز صدای کلیک مترونوم را از `app.js` جدا کند. مسئولیت ساختن `AudioContext`، `GainNode` و تولید ۴ نوع صدای مترونوم (classic, wood, beep, click) به این سرویس مستقل منتقل شد. `app.js` همچنان مالک state قدیمی `DAW.audioCtx` است و در صورت فعال بودن سرویس، تابع `playClick()` از طریق bridge به آن delegate می‌شود. مسیر legacy به‌عنوان fallback حفظ شده است.

### فایل‌ها و دلیل تغییر

- `js/core/AudioContextService.js`: سرویس مستقل برای lazily ایجاد/بازکردن AudioContext و سنتز صدای کلیک
- `js/app.js`: ساخت adapter و delegation تدریجی در `playClick()` بدون حذف منطق صوتی قدیمی
- `Akordyar.html`: attach ماژول بعد از `MetronomeEngine.js` و قبل از `app.js`
- `js/tests/audio-context-service.test.js`: تست سبک Node با FakeAudioContext برای سکوت بدون Ctx، ساخت گره‌ها، resume و مقادیر فرکانس

### اعتبارسنجی

- syntax فایل‌های JavaScript با `node --check`
- تست مستقل `node js/tests/audio-context-service.test.js`
- حفظ fallback قدیمی در `playClick()`
- بدون تغییر در transport، count-in، `checkMetronomeTick()` یا منطق DOM

### موارد باز برای مراحل بعدی

- طراحی look-ahead scheduling خارج از RAF (پیشنهاد در گزارش فاز قبل)
- انتقال کامل `ensureAudioCtx()` به سرویس پس از characterization
- حذف stateهای legacy فقط پس از جایگزین کامل

---

## فاز Safe Extraction موتور مترونوم

**وضعیت:** اتصال اولیه و bridge سازگار تکمیل شد؛ استخراج کامل هنوز انجام نشده است.

فایل `js/core/MetronomeEngine.js` در workspace موجود نبود و در مسیر طبیعی ماژول‌های core ایجاد شد. مسئولیت تشخیص عبور playhead به ضرب جدید و محاسبه accent به این موتور خالص منتقل شد. `app.js` همچنان مالک stateهای قدیمی `metroActive`، `metroTimer` و `metroBeat`، تولید صدای `playClick()`، count-in، transport و فراخوانی داخل حلقهٔ اصلی `tick()` است. توابع `startMetronome()`، `stopMetronome()` و `checkMetronomeTick()` به یک bridge حداقلی متصل شدند و مسیر محاسبه legacy در صورت نبود ماژول یا dependencyهای آن حفظ شده است.

### فایل‌ها و دلیل تغییر

- `js/core/MetronomeEngine.js`: موتور خالص و مستقل برای start/stop و تولید beat event
- `js/app.js`: ساخت adapter و delegation تدریجی بدون حذف state یا منطق صوتی قدیمی
- `Akordyar.html`: attach ماژول بعد از `Meter.js` و قبل از `app.js`
- `js/tests/metronome-engine.test.js`: تست سبک Node برای سکوت در حالت stop، عدم تکرار ضرب، accent و مترهای ترکیبی

### اعتبارسنجی

- syntax فایل‌های JavaScript با `node --check`
- تست مستقل `node js/tests/metronome-engine.test.js`
- تست برابری با فرمول legacy برای ۸ میزان‌نما و ۴ BPM
- حفظ fallback قدیمی در `checkMetronomeTick()`
- بدون تغییر در `tick()`، playback، Arranger، count-in، `playClick()` یا منطق DOM

### موارد باز برای مراحل بعدی

- لکنت مترونوم هنگام zoom/scroll همچنان open issue است؛ این مرحله scheduler را تغییر نداده است.
- انتقال تولید صدای `playClick()` به dependency صوتی مستقل
- طراحی look-ahead scheduling خارج از RAF پس از تثبیت bridge
- انتقال تدریجی count-in بدون تغییر رفتار transport
- حذف stateهای legacy فقط پس از characterization و جایگزین کامل

---

## فاز Look-ahead Scheduler مترونوم (MetronomeScheduler)

**وضعیت:** تکمیل شد — رفع لکنت مترونوم هنگام zoom/scroll با الگوی Chris Wilson.

فایل `js/core/MetronomeScheduler.js` ایجاد شد تا زمان‌بندی ضربات مترونوم را از حلقهٔ `requestAnimationFrame` جدا کند. به‌جای وابستگی به RAF در `tick()`، این scheduler با `setTimeout` کوتاه (پیش‌فرض ۲۵ms) اجرا می‌شود و ضربات را در `audioCtx.currentTime` از قبل رزرو می‌کند. این کار لکنت مترونوم هنگام zoom/scroll را برطرف می‌کند چون زمان‌بندی صدا دیگر به UI thread وابسته نیست.

### تغییرات انجام‌شده

- **`js/core/MetronomeScheduler.js`** (جدید): کلاس مستقل با الگوی look-ahead — `nextNoteTime`، `currentBeat` و `beatDuration` را مدیریت می‌کند و با `setTimeout` کوتاه ضربات را در پنجرهٔ `scheduleAheadTime` رزرو می‌کند.
  - **فیکس bind**: تمام متدهای داخلی (`start`، `stop`، `_scheduleNext`، `_scheduleBeat`، `_advanceBeat`، `getState`) در constructor با `bind(this)` بایند شدند تا context هرگز از بین نرود.
  - **فیکس setTimeout (Illegal invocation)**: `this._timer` حالا یک arrow-function wrapper دور `setTimeout` است: `this._timer = (fn, ms) => setTimeout(fn, ms)` تا فراخوانی `this._timer(...)` هرگز خطای "Illegal invocation" ندهد. `this._clearTimer` هم برای `clearTimeout` ساخته شد.
  - **فیکس زمان منفی (RangeError)**: `_nextNoteTime` در `start()` با `Math.max(0, this._startTime)` به عدد غیرمنفی clamped شد. `_startTime` اصلی برای `MetronomeEngine` حفظ می‌شود تا شمارش ضرب درست بماند، ولی `AudioParam.setValueAtTime` فقط عدد غیرمنفی می‌پذیرد.
  - **فیکس قطع صدا هنگام stop**: `stop()` حالا `audioContextService.stopAll()` را صدا می‌زند تا تمام نودهای صوتی که از قبل در آینده schedule شده‌اند (مثلاً ۱۰۰ms جلوتر) فوراً متوقف شوند و بعد از توقف مترونوم صدای اضافی پخش نشود.
  - **فیکس metroTimer**: `startMetronome()` در app.js حالا `metroTimer = true` را هم ست می‌کند تا `pauseTransport()`/`stopTransport()` بتوانند `stopMetronome()` را صدا بزنند و مترونوم واقعاً متوقف شود.
- **`js/core/AudioContextService.js`**: متد `playClickAt(isAccent, soundType, when)` اضافه شد تا کلیک در زمان دقیق AudioContext رزرو شود. `playClick()` حالا به `playClickAt()` delegate می‌کند.
  - **فیکس مدیریت نودهای فعال**: `_activeNodes` Set اضافه شد که هر نود صوتی ساخته‌شده را ثبت می‌کند. متد `stopAll()` تمام نودهای فعال را در `ctx.currentTime` متوقف و disconnect می‌کند. `_trackNode()` نودها را ثبت و بعد از اتمام طبیعی از Set حذف می‌کند.
  - `startMetronome()`/`stopMetronome()` به scheduler متصل شدند.
  - **فیکس همگام‌سازی playhead**: `startTime` در `startMetronome()` حالا از `ctx.currentTime - DAW.playhead` محاسبه می‌شود تا مترونوم از موقعیت فعلی playhead شروع شود و playhead هنگام شروع مترونوم متوقف نشود.
  - در حلقهٔ `tick()`، `checkMetronomeTick()` فقط وقتی فراخوانی می‌شود که scheduler در دسترس نباشد (fallback).
  - bridge `getMetronomeSchedulerBridge()` اضافه شد.
- **`Akordyar.html`**: script tag برای `MetronomeScheduler.js` بعد از `AudioContextService.js` و قبل از `app.js` اضافه شد.
- **`js/tests/metronome-scheduler.test.js`** (جدید): ۹ تست Node با FakeAudioContext و FakeTimer — اعتبارسنجی constructor، start/stop، look-ahead loop، تشخیص accent، استفاده از MetronomeEngine و state.

### اعتبارسنجی

- `node --check js/core/MetronomeScheduler.js`
- `node --check js/core/AudioContextService.js`
- `node --check js/app.js`
- `node js/tests/metronome-scheduler.test.js` → 10 passed
- `node js/tests/audio-context-service.test.js` → passed
- `node js/tests/metronome-engine.test.js` → passed

### موارد باز برای مراحل بعدی

- انتقال کامل `ensureAudioCtx()` به سرویس پس از characterization
- حذف stateهای legacy (`metroActive`، `metroTimer`، `metroBeat`) فقط پس از جایگزین کامل
- انتقال تدریجی count-in به scheduler بدون تغییر رفتار transport

---

## فاز Safe Extraction: ProjectAudioService

**وضعیت:** تکمیل‌شده — انتقال منطق import/load/resolve صوت از `app.js` با حفظ مالکیت state در DAW.

فایل `js/core/ProjectAudioService.js` ایجاد شد تا مسئولیت‌های I/O و restore صوتی پروژه از فایل monolithic `app.js` خارج شوند. این سرویس مالک `DAW` نیست؛ state از طریق dependency injection دریافت می‌شود و `app.js` همچنان مالک `DAW.audioCtx`، `DAW.masterGain`، `ensureAudioCtx()`، render و lifecycle کلی برنامه باقی می‌ماند.

### منطق منتقل‌شده

- `loadAudioFromHardDrive`
- `pathDirname`
- `pathJoin`
- `handleAudioImport`
- `loadProject`
- `resolveClipAudio`

### طراحی سازگاری

- برای جلوگیری از شکستن call-siteهای قدیمی، wrapperهای نازک با نام‌های قبلی در `app.js` باقی ماندند.
- APIهای legacy شامل `window.loadAudioFromHardDrive`، `window.pathDirname` و `window.pathJoin` حفظ شدند.
- `ensureAudioCtx()` و مالکیت `DAW.audioCtx` بدون تغییر باقی ماندند.
- `renderTimeline()` و loading indicator به شکل callback به سرویس تزریق شدند.
- سرویس برای Electron API شامل `checkFileExists`، `readAudioFile` و `resolvePath` dependency injection دارد.

### تست افزوده‌شده

فایل `js/tests/project-audio-service.test.js` اضافه شد و این موارد را پوشش می‌دهد:

- path dirname و path join
- load فایل صوتی موجود و فایل غایب
- decode صوت با FakeAudioContext
- import فایل و ایجاد track/clip
- resolve مسیرهای copy و reference
- restore کردن pool، کلیپ‌های قدیمی و state پروژه

### اعتبارسنجی

- `node --check js/core/ProjectAudioService.js`
- `node --check js/app.js`
- `node js/tests/project-audio-service.test.js`
- `node js/tests/audio-context-service.test.js`
- `node js/tests/metronome-engine.test.js`
- `node js/tests/metronome-scheduler.test.js`
- `git diff --check`

### نتیجه

بخش منسجم import/load/resolve صوت از `app.js` خارج شد و مسیر استخراج‌های پرحجم بعدی، به‌خصوص Clipboard/Paste logic و Lyric parser integration، بدون دست‌زدن به transport/playback آماده‌تر شد.

---

## یادداشت‌ها

- فایل‌های untracked نامرتبط (f.py، fix2.py، ...) نباید دست زده شوند
- js/performanceStore.js، js/songDocumentModel.js، preload.js، server.js تغییرات pre-existing دارند
- تست characterization در js/tests/characterization-tests.js و با js/tests/test.html اجرا می‌شود
