# گزارش کامل روند ویرایش و ساماندهی پروژه آکوردیار

تاریخ: ۱۴۰۵/۰۵/۲۰  
موضوع: تفکیک و ایمن‌سازی فایل اصلی `app.js`

## خلاصهٔ اجرایی

در این مرحله فایل بسیار بزرگ `js/app.js` که شامل ۱۲۹۶۱ خط کد بود، بدون تغییر در منطق عملکردی به چهار بخش مستقل و قابل نگهداری تقسیم شد.

فایل اصلی اکنون فقط یک bootstrap سازگار با نسخه‌های قدیمی است و ۵۱ خط دارد. فایل اصلی HTML نیز مستقیماً بخش‌ها را با ترتیب مشخص بارگذاری می‌کند.

این مرحله عمداً با کمترین ریسک انجام شد؛ یعنی ابتدا ساختار پروژه مرتب شد و سپس خطاهای ناشی از وابستگی بین بخش‌ها شناسایی و اصلاح شدند.

## وضعیت قبل از تغییر

- تمام منطق DAW، ویرایشگر، چاپ و جستجو در `js/app.js` متمرکز بود.
- تعداد خطوط `app.js`: حدود ۱۲۹۶۱ خط
- نگهداری، پیدا کردن خطا و تشخیص وابستگی‌ها دشوار بود.
- اجرای مستقیم بعضی کدها به hoisting و scope فایل یکپارچه وابسته بود.
- ابزار `js/fix-grid.js` نیز مستقیماً فایل بزرگ `app.js` را تغییر می‌داد.

## ساختار جدید

کد اصلی به این چهار بخش تقسیم شد:

### `js/app/core.js`

شامل:

- راه‌اندازی محیط Electron و Browser
- وضعیت DAW
- تایم‌لاین و ترک‌ها
- صدا، فایل‌های صوتی و waveform
- ذخیره و بارگذاری state
- حمل‌ونقل، loop و playhead
- مترو‌نوم و grid
- bridgeهای سرویس‌های اصلی

### `js/app/editor.js`

شامل:

- ویرایشگر متن و آکورد
- انتخاب و جابه‌جایی آکوردها
- transpose و chord editor
- sync خطوط ترانه
- MIDI editor
- arranger
- تنظیمات و shortcutها
- import/export ترانه

### `js/app/print.js`

شامل منطق چاپ ترانه با حفظ جایگاه دقیق آکوردها و پشتیبانی از Electron و Browser.

### `js/app/search.js`

شامل پنل جستجوی سریع، فیلترها، مرتب‌سازی و بارگذاری ترانه‌ها.

## تغییرات بارگذاری

در `Akordyar.html` بارگذاری قبلی:

```html
<script src="js/app.js"></script>
```

به ترتیب مشخص زیر تبدیل شد:

```html
<script src="js/app/core.js"></script>
<script src="js/app/editor.js"></script>
<script src="js/app/print.js"></script>
<script src="js/app/search.js"></script>
```

ترتیب وابستگی‌ها عمداً حفظ شده است:

```text
core → editor → print → search
```

فایل `js/app.js` نیز به عنوان compatibility entry point باقی مانده است تا صفحاتی که هنوز آن را مستقیماً load می‌کنند، قابلیت سازگاری داشته باشند.

## خطاهای شناسایی‌شده و اصلاح‌شده

### خطای `edSelectedChords`

خطا:

```text
Cannot access 'edSelectedChords' before initialization
```

علت، مقداردهی متغیر انتخاب آکورد بعد از ثبت event handler بود. متغیر به ابتدای `editor.js` منتقل شد تا handler حتی در صورت شکست بخشی از initialization نیز به متغیر uninitialized دسترسی نداشته باشد.

### خطای `edSaveSong` در HistoryService

خطا:

```text
edSaveSong is not defined
```

علت این بود که `attachHistoryService()` در پایان `core.js`، قبل از اجرای کامل `editor.js` اجرا می‌شد.

اصلاح:

- فراخوانی `attachHistoryService()` به انتهای `editor.js` منتقل شد.
- در این نقطه تمام توابع و stateهای ویرایشگر آماده هستند.

### خطای `timeToX`

خطا:

```text
Cannot access 'timeToX' before initialization
```

این خطا پیامد توقف اجرای `core.js` در خطای قبلی بود. پس از انتقال initialization، `core.js` تا مقداردهی `timeToX` کامل اجرا می‌شود.

### خطای `ClipboardService`

خطا در زمان ساخت سرویس clipboard رخ می‌داد، چون سرویس هنگام load شدن `core.js` به `edSaveSong` نیاز داشت.

اصلاح:

- ساخت `ClipboardService` به صورت lazy انجام شد.
- سرویس فقط هنگام اولین استفاده و پس از آماده‌شدن editor ساخته می‌شود.

### خطای `syncModeControllerBridge`

خطا:

```text
Cannot access 'syncModeControllerBridge' before initialization
```

اصلاح:

- bridge از `const` با ساخت فوری به lazy bridge تبدیل شد.
- `SyncModeController` فقط در زمان نیاز و بعد از آماده‌شدن dependencyها ساخته می‌شود.

## اصلاح ابزارهای جانبی

فایل زیر نیز با ساختار جدید هماهنگ شد:

```text
js/fix-grid.js
```

مسیر هدف آن از:

```text
js/app.js
```

به:

```text
js/app/core.js
```

تغییر کرد تا ابزار grid بعد از refactor همچنان قابل استفاده باشد.

## اصلاح مستندات

در `documentation.html`، ساختار جدید `js/app/` و نقش چهار ماژول اصلی ثبت شد تا مستندات با وضعیت واقعی پروژه همگام باشد.

## بررسی صحت و تست‌ها

بررسی syntax برای فایل‌های زیر موفق بود:

- `js/app.js`
- `js/app/core.js`
- `js/app/editor.js`
- `js/app/print.js`
- `js/app/search.js`
- `js/fix-grid.js`

تست اصلی پروژه:

```text
126 passed, 0 failed
```

همچنین:

- `git diff --check` بدون خطای محتوایی اجرا شد.
- تمام بخش‌های split شده از نظر syntax معتبر هستند.
- منطق چهار فایل جدید از نسخهٔ قبلی استخراج شده و تغییر رفتاری عمدی در آن انجام نشده است.

## نتیجهٔ این مرحله

دستاوردهای اصلی:

1. `app.js` از ۱۲۹۶۱ خط به ۵۱ خط کاهش یافت.
2. مسئولیت‌ها به چهار بخش مشخص تقسیم شدند.
3. ترتیب بارگذاری dependencyها صریح شد.
4. وابستگی‌های initialization بین core و editor ایمن شدند.
5. ابزارهای نگهداری و documentation با ساختار جدید هماهنگ شدند.
6. خطاهای runtime گزارش‌شده اصلاح شدند.

نکتهٔ مهم: در این مرحله حجم مجموع منطق برنامه عمداً تقریباً ثابت مانده است؛ کد ابتدا از نظر ساختاری تفکیک شده تا ریسک کاهش واقعی منطق کم شود.

## برنامهٔ مرحلهٔ بعد

برای رسیدن به هدف نهایی «چندصد خط کد اصلی»، مراحل بعدی باید به صورت تدریجی انجام شوند:

- استخراج کامل Audio/Transport به سرویس‌های مستقل
- استخراج History و Project State
- استخراج MIDI و Metronome
- استخراج Timeline Renderer و Clip Operations
- کاهش global state و تبدیل bridgeها به APIهای مشخص
- حذف کدهای تکراری و compatibility wrapperهای قدیمی
- اضافه‌کردن تست characterization برای مسیرهای حساس
- اجرای smoke test واقعی در Electron بعد از هر مرحله

این تفکیک مرحله‌ای باعث می‌شود کاهش حجم واقعی بدون از دست‌دادن قابلیت‌های فعلی و بدون ایجاد regression انجام شود.

## وابستگی‌های تحت پایش مستمر

موارد زیر همچنان debt معماری محسوب می‌شوند و باید در هر مرحلهٔ بعدی پایش، مستندسازی و تا حد امکان کاهش داده شوند:

| وابستگی | وضعیت فعلی | هدف کاهش |
|---|---|---|
| `window.edCur` | مدل ترانه از طریق global scope بین بخش‌ها عبور می‌کند. | تبدیل به state accessor یا store مشخص با API محدود |
| `DAW` | state اصلی DAW در چندین تابع و handler به‌صورت مستقیم مصرف می‌شود. | تفکیک state، commandها و rendererها |
| `PERF` | state اجرای performance در سطح global در دسترس است. | محدودکردن دسترسی به PerformanceStore/Bridge |
| `DomainBridge` | پل بین منطق دامنه و UI هنوز بخشی از وابستگی‌های legacy را نگه می‌دارد. | تعریف قراردادهای کوچک و حذف دسترسی‌های مستقیم |
| global event handlers | بخشی از رویدادها با `document`، `window` و handlerهای inline ثبت می‌شوند. | انتقال به EventBindings متمرکز و حذف handlerهای پراکنده |

### روش پایش

در هر commit مربوط به refactor باید این موارد بررسی شوند:

1. آیا global جدیدی اضافه شده است؟
2. آیا مصرف‌کنندهٔ جدیدی برای `window.edCur`، `DAW` یا `PERF` ایجاد شده است؟
3. آیا handler جدیدی مستقیماً روی `window` یا `document` ثبت شده است؟
4. آیا یک dependency قدیمی حذف یا به accessor محدود تبدیل شده است؟
5. آیا تغییر باعث افزایش coupling بین `core.js` و `editor.js` شده است؟

معیار موفقیت مرحلهٔ بعد، فقط کم‌شدن تعداد خطوط نیست؛ باید تعداد نقاط دسترسی مستقیم به این وابستگی‌ها نیز کاهش پیدا کند.

## اجرای مرحلهٔ اول پایش

در نخستین گام عملی، مسیر `edCur` در بخش چاپ اصلاح شد:

- `js/app/print.js` دیگر مستقیماً `edCur` را نمی‌خواند.
- خواندن song فعلی از طریق `window.EdCurAdapter.getEdCur()` انجام می‌شود.
- assignmentهای مستقیم `window.edCur` در `core.js` و `editor.js` حذف شدند.
- نگهداری reference سازگاری `window.edCur` اکنون از طریق `EdCurAdapter.setEdCur()` انجام می‌شود.

نتیجهٔ اندازه‌گیری این مرحله:

```text
assignment مستقیم window.edCur در app/core/editor: ۷ مورد → ۰ مورد
تعداد globalهای جدید: ۰
تست parser: ۱۲۶ موفق، ۰ ناموفق
```

## اجرای مرحله دوم: کاهش coupling در DAW و PERF

در این مرحله، ابتدا یک مصرف‌کننده مستقل و کم‌ریسک انتخاب شد:
`js/performanceBridge.js`.

تغییرات انجام‌شده:

- دسترسی مستقیم این فایل به `edCur` حذف و خواندن آهنگ فعلی به `EdCurAdapter.getEdCur()` منتقل شد.
- دسترسی مستقیم به `DAW` حذف و از `RuntimeStateAdapter.getDAW()` استفاده شد.
- دسترسی مستقیم به `PerformanceStore` حذف و از `RuntimeStateAdapter.getPerformanceStore()` استفاده شد.
- فایل جدید `js/core/RuntimeStateAdapter.js` به‌عنوان نقطه دسترسی کنترل‌شده اضافه شد.
- نمونه واقعی `DAW` و `PERF` پس از initialization در `app/core.js` به adapter قابل دسترسی شد؛ ترتیب initialization قبلی حفظ شد.
- ترتیب بارگذاری HTML با اضافه‌شدن adapter، قبل از `performanceStore` و `performanceBridge`، مشخص شد.

نتیجه اندازه‌گیری این بخش:

```text
ارجاع مستقیم edCur/DAW/PerformanceStore در performanceBridge.js:
قبل: وجود داشت
بعد: ۰ ارجاع اجرایی؛ فقط نام‌ها در توضیحات فایل باقی مانده‌اند.
```

اعتبارسنجی مرحله:

- `npm test`: تعداد ۱۲۶ موفق و ۰ ناموفق.
- `node --check`: فایل adapter، performance bridge، چهار بخش app و EdCurAdapter بدون خطای syntax.
- تست‌های مستقل AudioContextService، Characterization دامنه، MetronomeEngine و ProjectAudioService موفق بودند.
- دو تستی که مستقیماً با `node file.js` اجرا می‌شوند و به harness مرورگر نیاز دارند، خارج از محیط مناسب خود قابل اجرای مستقل نیستند.
- تست MetronomeScheduler به‌دلیل timer باز در harness به timeout رسید؛ این موضوع به تغییرات این مرحله مرتبط تشخیص داده نشد و باید در پاک‌سازی تست‌ها پیگیری شود.

این adapter عمداً یک API کوچک نگه داشته شده است تا در مراحل بعد، مصرف‌کننده‌های `WaveformService`، کنترلرهای editor و handlerهای پراکنده نیز بدون ایجاد global جدید به آن منتقل شوند.

## اجرای مرحله سوم: استانداردسازی سرویس‌های Sync و Waveform

در این مرحله دو سرویس مستقل از state سراسری جدا شدند:

### SyncModeController

- `PerformanceStore`، `SharedEngine` و `_songDocument` دیگر به‌صورت مستقیم از bindingهای global خوانده نمی‌شوند.
- کنترلر از طریق `getPerformanceStore()` به store دسترسی می‌گیرد.
- سند آهنگ از state store خوانده می‌شود.
- دسترسی به `window` برای handler کلید Sync از طریق `windowRef` تزریق می‌شود.
- وابستگی اصلی `DAW` همچنان از constructor تزریق می‌شود و کنترلر مالک آن نیست.

### WaveformService

- fallback مستقیم به `global.DAW` و `global.ensureAudioCtx` حذف شد.
- `ensureAudioCtx`، `setAudioContext` و `getWaveCache` به‌عنوان dependency صریح تعریف شدند.
- دسترسی به DOM از طریق `documentRef` انجام می‌شود.
- سرویس اکنون در Node نیز قابل import و تست است.
- تست مستقل `js/tests/waveform-service.test.js` اضافه شد.

### DomainBridge و EdCurAdapter

- دسترسی مستقیم این دو bridge به `PerformanceStore` به `RuntimeStateAdapter` منتقل شد.
- ترتیب scriptها طوری اصلاح شد که adapter قبل از مصرف‌کننده‌های آن بارگذاری شود.

نتیجه تست این مرحله:

```text
parser: ۱۲۶ موفق، ۰ ناموفق
SyncModeController: ۱۵ موفق، ۰ ناموفق
WaveformService: موفق
node --check فایل‌های مرحله: موفق
```

این مرحله یک الگوی مشخص برای ادامه refactor ایجاد کرد: سرویس‌ها باید dependencyهای runtime را از constructor یا accessor محدود دریافت کنند، نه از bindingهای آزاد و global.

## اجرای مرحله چهارم: متمرکزسازی handlerهای سراسری editor

دو handler عمومی که قبلاً مستقیماً داخل `js/app/editor.js` ثبت می‌شدند، به
`EventBindings` منتقل شدند:

- `keyup` برای آزادکردن راهنمای برش هنگام رهاکردن کلید Shift
- `mousedown` در capture phase برای فعال‌کردن حالت MIDI/button mapping با
  ترکیب Ctrl+Shift+Alt

تغییرات معماری:

- `EventBindings` اکنون `onGlobalKeyup` و `onGlobalMousedownCapture` را پشتیبانی می‌کند.
- همه listenerهای این دو مسیر مانند سایر listenerهای EventBindings در `destroy()`
  قابل حذف هستند.
- مقدارهای پیش‌فرض `documentRef` و `windowRef` در محیط Node نیز امن شدند.
- export ماژول برای تست مستقیم اضافه شد.
- تست `js/tests/event-bindings.test.js` اضافه شد.

نتیجه:

```text
EventBindings: موفق
editor.js: دو ثبت مستقیم global حذف شد
parser: ۱۲۶ موفق، ۰ ناموفق
```

ثبت‌های باقی‌مانده در `editor.js` عمدتاً listenerهای موقت drag، scrub، منوی
زمینه و lifecycleهای مستقل هستند و در مرحله بعد به scopeهای قابل پاک‌سازی
منتقل می‌شوند.

## اصلاح باگ حذف فایل صوتی با کلید Delete

خطای گزارش‌شده:

```text
ClipboardService.js:23
TypeError: stopAllVoices is not a function
```

علت این بود که bridge ساخت `ClipboardService`، callback مربوط به
`stopAllVoices` را در dependency context سرویس قرار نداده بود. در نتیجه، هنگام
حذف کلیپ صوتی، اجرای حذف قبل از پاک‌سازی state متوقف می‌شد.

اصلاح انجام‌شده:

- `stopAllVoices` به context ساخت `ClipboardService` اضافه شد.
- `ClipboardService.deleteSelected()` در صورت نبود callback، fallback بی‌خطر
  دارد و دیگر با TypeError متوقف نمی‌شود.
- export ماژولی سرویس برای تست مستقیم اضافه شد.
- تست `js/tests/clipboard-service.test.js` اضافه شد.

نتیجه:

```text
ClipboardService: موفق
حذف کلیپ صوتی و section: موفق
parser: ۱۲۶ موفق، ۰ ناموفق
```

این الگو در مراحل بعدی برای stateهای `DAW` و `PERF` نیز با احتیاط و به‌صورت بخش‌بندی‌شده تکرار خواهد شد.

## اصلاح encoding متن‌های فارسی

پس از اجرای نسخهٔ split‌شده، مشخص شد که بخشی از متن‌های فارسیِ dynamic در چهار فایل `js/app/*` به شکل mojibake ذخیره شده‌اند؛ نمونه‌هایی مانند `Ø...` و `Ù...` در صفحه دیده می‌شدند.

بررسی ریشهٔ مشکل:

- `Akordyar.html` و متن‌های فارسی ثابت آن UTF-8 سالم داشتند.
- فایل‌های `js/app/core.js`، `editor.js`، `print.js` و `search.js` دارای متن فارسیِ تبدیل‌شده و خراب بودند.
- مشکل از charset مرورگر یا فونت نبود؛ خود source JavaScript خراب بود.

اقدام انجام‌شده:

- متن‌های خراب با تبدیل کنترل‌شدهٔ Windows-1252/UTF-8 به فارسی اصلی برگردانده شدند.
- منطق JavaScript و اصلاحات قبلی حفظ شدند.
- فایل‌های JavaScript از نظر syntax بررسی شدند.
- scan کل پروژه انجام شد؛ markerهای mojibake در چهار فایل app به صفر رسیدند.

نتیجه:

```text
متن فارسی dynamic: mojibake → UTF-8 سالم
تست parser: ۱۲۶ موفق، ۰ ناموفق
```

## رفع باگ نوار ابزار در اجرای Electron

در اجرای Electron نوار ابزار `header-center-controls` در HTML وجود داشت، اما دو عامل باعث می‌شد کاربر آن را نبیند یا راه‌اندازی ابزارها ناقص شود:

- تابع `initAccidentalSelector` میزبان واقعی را در متغیر `host` پیدا می‌کرد، اما در پایان به متغیر تعریف‌نشدهٔ `header` دسترسی داشت و با خطای `ReferenceError` متوقف می‌شد.
- Project Hub در شروع Electron به‌صورت overlay روی کل ادیتور باز می‌شد و نوار ابزار را پشت خود می‌پوشاند.

اصلاح‌ها:

- الحاق selector دیز/بمل به `host` واقعی (`headerCenterControls`) انجام شد.
- startup خودکار Project Hub برای Electron غیرفعال شد؛ خود Hub همچنان از مرکز پروژه قابل بازشدن است.

اعتبارسنجی:

```text
node --check js/app/editor.js: موفق
node --check js/projecthub.js: موفق
parser test: ۱۲۶ موفق، ۰ ناموفق
```

## کاهش وابستگی مستقیم ArchiveModule به `window.edCur`

در audit بعدی مشخص شد که مسیرهای واردکردن پروژه، ساخت نسخهٔ قابل‌ویرایش، ساخت ترانهٔ جدید و بارگذاری فایل پروژه در `ArchiveModule.js` هنوز مستقیماً `window.edCur` را بازنویسی می‌کردند.

این چهار نقطه به `EdCurAdapter.setEdCur()` منتقل شدند. در نتیجه:

- مرجع legacy همچنان برای کدهای قدیمی حفظ می‌شود.
- مالکیت همگام‌سازی `edCur` در یک adapter باقی می‌ماند.
- تعداد writerهای مستقیم `window.edCur` در ArchiveModule به صفر رسید.
- رفتار آرشیو، import و ساخت ترانه تغییر نکرد.

اعتبارسنجی:

```text
ArchiveModule direct window.edCur writers: ۰
parser test: ۱۲۶ موفق، ۰ ناموفق
```

## پایدارسازی تست‌های مستقل سرویس‌ها

در اجرای کامل تست‌های مستقل، دو مشکل زیرساختی مشخص شد که به منطق آرشیو مربوط نبودند:

- `metronome-scheduler.test.js` در یک سناریو از timer واقعی استفاده می‌کرد و به‌دلیل باقی‌ماندن timer، فرایند Node خاتمه پیدا نمی‌کرد.
- `sync-analysis.test.js` فایل browser-first را بدون load کردن ماژول اجرا می‌کرد و با `SyncAnalysis is not loaded` متوقف می‌شد.

اصلاح شد:

- سناریوی scheduler از fake timer استفاده می‌کند و scheduler را صریحاً متوقف می‌کند.
- `SyncAnalysis.js` خروجی CommonJS امن برای تست Node دارد و تست آن را مستقیم require می‌کند.

نتیجهٔ تست‌های مستقل:

```text
همهٔ تست‌های سرویس‌ها: موفق
```

## کاهش وابستگی Project Hub به stateهای سراسری

مسیر اعمال قالب در `projecthub.js` تنها بخش این ماژول بود که مستقیماً به `edCur` و `DAW` متکی می‌شد.

این مسیر اکنون از دو accessor کنترل‌شده استفاده می‌کند:

- `EdCurAdapter.getEdCur()` برای song جاری
- `RuntimeStateAdapter.getDAW()` برای state تایم‌لاین

منطق ساخت قالب، ساخت nodeهای صوتی، تنظیم طول تایم‌لاین و رندر UI حفظ شد؛ فقط نحوهٔ دسترسی به state تغییر کرد.

اعتبارسنجی:

```text
ProjectHub direct executable edCur/DAW access: ۰
parser: ۱۲۶ موفق، ۰ ناموفق
تمام تست‌های مستقل: موفق
```

## محدودکردن handlerهای سراسری آرشیو

دو listener مربوط به بستن منوهای context در `ArchiveModule.js` روی `document` ثبت شده بودند، درحالی‌که هر دو فقط به محیط modal آرشیو مربوط بودند.

این listenerها به `#archiveModal` منتقل شدند:

- منوی context اصلی آرشیو
- منوی context هنرمندان

با این تغییر، رفتار کلیک بیرون از منو حفظ شد، اما رویداد دیگر روی کل سند پخش نمی‌شود و scope آن به رابط آرشیو محدود است.

handlerهای `mousemove` و `mouseup` مربوط به drag/resize عمداً برای مرحلهٔ جداگانه باقی ماندند، چون تا پایان drag ممکن است pointer از محدودهٔ عنصر خارج شود.

## تکمیل حذف handlerهای global و PERF از آرشیو

در ادامهٔ همین مسیر:

- resize divider هنرمندان از `document.mousemove/mouseup` به Pointer Events و `setPointerCapture` منتقل شد.
- آخرین سه writer مستقیم `PERF.lastSerializedState` از `ArchiveModule.js` حذف شدند و در تابع `resetPerformanceSerialization()` متمرکز شدند.

نتیجه:

```text
ArchiveModule document mousemove/mouseup listeners: ۰
ArchiveModule executable PERF access: ۰
```

## استانداردسازی runner تست و بودجهٔ وابستگی

برای جلوگیری از برگشت وابستگی‌های حذف‌شده و پراکندگی تست‌ها:

- تست `runtime-dependency-budget.test.js` اضافه شد.
- این تست writer مستقیم `window.edCur`، دسترسی اجرایی `PERF` و handlerهای global آرشیو را کنترل می‌کند.
- دسترسی مستقیم Project Hub به `edCur`/`DAW` و PerformanceBridge به stateهای legacy نیز پایش می‌شود.
- `npm test` اکنون از `js/tests/run-all-tests.js` استفاده می‌کند و parser به‌همراه همهٔ تست‌های مستقل سرویس‌ها را اجرا می‌کند.

از این مرحله به بعد، هر commit معماری باید از runner کامل عبور کند.

## ایجاد مرز ArchiveRuntimeAdapter و کاهش دسترسی مستقیم آرشیو به DAW

برای اینکه `ArchiveModule.js` به متغیرهای legacy وابسته نماند، قرارداد
`ArchiveRuntimeAdapter` اضافه شد. این adapter دسترسی‌های زیر را در یک نقطه
متمرکز می‌کند:

- song جاری از مسیر `EdCurAdapter`
- state تایم‌لاین از مسیر `RuntimeStateAdapter`
- state عملکرد و reset کردن serialization

تمام ارجاع‌های مستقیم آرشیو به `DAW` به `getArchiveDAW()` منتقل شد و در
مسیرهای سنگین مانند load، save، import، export و ساخت ترانهٔ جدید، reference
محلی `daw` استفاده می‌شود. این کار هم وابستگی legacy را محدود می‌کند و هم
lookupهای تکراری را کاهش می‌دهد.

برای جلوگیری از بازگشت وابستگی:

- تست قرارداد `archive-runtime-adapter.test.js` اضافه شد.
- تست بودجهٔ وابستگی، نبود دسترسی مستقیم آرشیو به `DAW`، `PERF` و
  `window.edCur` را کنترل می‌کند.
- adapter در محیط Node نیز قابل بارگذاری و تست است.

نتیجهٔ اعتبارسنجی:

```text
node --check فایل‌های تغییرکرده: موفق
git diff --check: موفق
npm test: هر ۱۳ ورودی تست موفق
```

## batch سی‌مرحله‌ای: کاهش handlerهای سراسری و استانداردسازی runtime

در این batch، مراحل زیر به‌صورت پیوسته انجام شد:

- baseline، ساختار فایل‌ها، وابستگی‌های `window.edCur`، `DAW`، `PERF`,
  `DomainBridge`، Electron و encoding ممیزی شد.
- drag پنل جست‌وجو به Pointer Events و `setPointerCapture` منتقل شد و از
  ثبت چندبارهٔ listener با guard داخلی جلوگیری شد.
- drag نوار ابزار، scrub پلی‌هد، تغییر ارتفاع timeline، drag chord و separator
  timeline از listenerهای `document.mousemove/mouseup` جدا شدند.
- keyboard handler مربوط به Project Hub از `document` به خود hub منتقل شد و
  focus قابل‌کنترل برای آن اضافه شد.
- `RuntimeStateAdapter` متدهای `getElectronAPI`، `getDAWOrThrow` و
  `getPERFOrThrow` دریافت کرد.
- `ArchiveModule` در نبود `ArchiveRuntimeAdapter` خطای واضح و قابل‌ردیابی
  می‌دهد و دیگر failure را silent رها نمی‌کند.
- `HistoryService` و `SyncModeController` به getterهای runtime قابل تزریق
  منتقل شدند و برای تست Node نیز CommonJS-safe شدند.
- تست‌های قرارداد adapter، تست بودجهٔ وابستگی، تست ترتیب بارگذاری scriptها و
  تست سلامت encoding اضافه شدند.

نتیجهٔ این batch:

```text
npm test: هر ۱۷ ورودی تست موفق
node --check فایل‌های تغییرکرده: موفق
git diff --check: موفق
document.mousemove/mouseup در editor و search: حذف شد
keyboard handler سراسری Project Hub: حذف شد
```

## batch چهارم: مرز runtime برای DAW/PERF و درگ‌های Pointer

در این مرحله وابستگی‌های باقی‌مانده در مسیر اصلی ادیتور کاهش داده شد:

- `EditorRuntimeAdapter` به‌عنوان مرز lazy برای `DAW`، `PERF`، song و `PerformanceStore` تکمیل شد.
- همهٔ دسترسی‌های اجرایی مستقیم `DAW.` و `PERF.` در `js/app/core.js` و `js/app/editor.js` به getterهای runtime منتقل شدند.
- `SyncModeController` دیگر `this.DAW` ثابت نگه نمی‌دارد و در هر عملیات `getDAW()` را می‌خواند.
- `ClipboardService` از `getDAW()` پشتیبانی می‌کند و در صورت نامعتبر بودن `stopAllVoices` خطا نمی‌دهد؛ این بخش باگ حذف کلیپ صوتی را مقاوم‌تر می‌کند.
- `DomainBridge` به یک bridge مستقل، lazy و CommonJS-safe تبدیل شد و برای callbackهای runtime، song و `PerformanceStore` قرارداد مشخص دارد.
- درگ‌های pan، resize ترک، resize section/clip، header، mixer، locator loop، پنل arranger و پنل performance به `Pointer Events` و `startEditorPointerDrag` منتقل شدند.
- وابستگی‌های `document.mousemove/mouseup` در `core`، `editor`، `search` و مسیرهای بررسی‌شده حذف شدند.
- برای جلوگیری از برگشت معماری قبلی، تست قرارداد adapter، تست Pointer Drag و تست بودجهٔ وابستگی اضافه/به‌روزرسانی شد.

نتیجهٔ اندازه‌گیری این batch:

```text
core.js direct DAW property access: 405 → 0
editor.js direct DAW property access: 259 → 0
core.js direct PERF property access: 5 → 0
editor.js direct PERF property access: 3 → 0
core/editor document mousemove/mouseup handlers: حذف شد
global window.edCur access در مسیرهای اصلی: 0
```

اعتبارسنجی:

```text
npm test: 19 ورودی تست موفق
node --check فایل‌های تغییرکرده: موفق
git diff --check: موفق
```

## عملیات ۱۰۰تایی: پایداری داده، encoding و مدیریت منابع

در این دور، بهبودهای زیر انجام شد:

- `TextEncodingService` برای ترمیم محافظه‌کارانهٔ متن‌های UTF-8 خراب‌شده اضافه شد.
- ترمیم متن هنگام بارگذاری state، آهنگ ذخیره‌شده و آهنگ‌های ارنجر فعال شد.
- `ProjectAudioService.loadProject` دادهٔ نامعتبر را زود رد می‌کند.
- loading indicator در موفقیت و خطای داخلی همیشه پاک می‌شود.
- مسیرهای clipboard با `Map` و object cache هر دو سازگار شدند.
- انتخاب‌های clipboard همیشه با `Set` ذخیره می‌شوند.
- `performanceBridge` در بستن popup، interval، subscription، RAF و BroadcastChannel را پاک‌سازی می‌کند.
- چند مسیر داغ در `core.js` با lookup کمتر و متغیر محلی runtime مرتب شدند.
- تست‌های encoding، loader، clipboard و قراردادهای runtime توسعه پیدا کردند.

معیار نهایی:

```text
core.js direct DAW/PERF: 0/0
editor.js direct DAW/PERF: 0/0
core/editor window.edCur: 0
core/editor document.mousemove/mouseup: 0
npm test: 20 ورودی تست موفق
syntax همهٔ فایل‌های js/: موفق
```

## رفع مشکل اتصال Player View گوشی در اجرای Electron

### شرح مشکل

قابلیت گروه‌نوازی و نمایش `Player View` روی گوشی با اجرای مستقیم `start.bat`
کار می‌کرد، اما با اجرای اصلی برنامه از طریق `Run-Akordyar.bat` گوشی وصل نمی‌شد.
علت این بود که در اجرای Electron، رابط دسکتاپ و سرور Express داخل یک فرایند
اجرا می‌شدند؛ درحالی‌که اجرای مستقیم `start.bat`، سرور Node را به‌صورت مستقل
روی `0.0.0.0:3000` اجرا می‌کرد و گوشی به همان سرور دسترسی داشت.

### علت‌های شناسایی‌شده

- `Run-Akordyar.bat` ابتدا سرور قبلی پورت ۳۰۰۰ را می‌بست و Electron سرور را
  داخل خودش load می‌کرد؛ این مسیر با مسیر موفق `start.bat` یکسان نبود.
- کلاینت `masterSync` به پیام heartbeat از نوع `PING` پاسخ `PONG` نمی‌داد.
  بنابراین `SyncHub` بعد از دو بازهٔ heartbeat، اتصال Laptop را مرده تشخیص
  می‌داد و با کد `1006` قطع می‌کرد.
- هنگام اجرای سرور مستقل از Electron، `server.js` حالت اجرا را وب تشخیص می‌داد
  و `browser-opener.js` صفحهٔ `Akordyar.html` را ناخواسته در مرورگر باز می‌کرد.
- در فرایند عیب‌یابی، یک خطای محدودهٔ متغیر در `masterSync.js` با عنوان
  `ReferenceError: socket is not defined` نیز ایجاد شد که باعث ناقص‌شدن handshake
  و باقی‌ماندن peer در حالت `pending` می‌شد.

### اصلاحات انجام‌شده

- `Run-Akordyar.bat` اکنون `server.js` را مانند `start.bat` در یک پردازش Node
  جداگانه اجرا می‌کند و Electron فقط رابط دسکتاپ را باز می‌کند.
- در `masterSync.js`، پاسخ خودکار `PONG` به heartbeat سرور اضافه شد.
- در `mobileClient.js` نیز پاسخ `PONG` اضافه شد تا اتصال گوشی پایدار بماند.
- مدیریت متغیر socket در `masterSync.js` اصلاح و خطای `socket is not defined`
  برطرف شد.
- برای اجرای دسکتاپ، متغیر محیطی `AKORDYAR_DESKTOP=1` اضافه شد تا سرور مستقل
  مرورگر سیستم را به‌صورت خودکار باز نکند.
- سرور همچنان روی `0.0.0.0:3000` گوش می‌دهد و آدرس گوشی از IP شبکهٔ لپ‌تاپ
  ساخته می‌شود؛ نمونه:

```text
http://10.118.123.228:3000/sync-client.html
```

### نتیجه

پس از این اصلاحات:

- اتصال Electron به SyncHub پایدار شد و چرخهٔ تکراری `connect/disconnect` حذف شد.
- اجرای `Run-Akordyar.bat` مانند `start.bat` امکان اتصال گوشی را فراهم می‌کند.
- صفحهٔ اضافی مرورگر در حالت اجرای دسکتاپ حذف شد.
- فایل‌های مرتبط با این اصلاح:
  - `Run-Akordyar.bat`
  - `server.js`
  - `electron-main.js`
  - `js/sync/masterSync.js`
  - `js/sync/mobileClient.js`
  - `server/syncHub.js`

اعتبارسنجی نهایی:

```text
node --check server.js: موفق
node --check electron-main.js: موفق
node --check js/sync/masterSync.js: موفق
node --check js/sync/mobileClient.js: موفق
node --check server/syncHub.js: موفق
```

## مرحلهٔ ۳۱: استخراج Chord Line Popup

در این مرحله تابع بزرگ `syncChordLinePopup` از `js/app/core.js` خارج شد و به
سرویس مستقل `CoreChordLinePopupService` منتقل شد. این سرویس مسئولیت‌های زیر را
در یک مرز قابل‌آزمون نگه می‌دارد:

- ساخت و نمایش پنجرهٔ Chord Line
- رندر متن و آکوردها با درنظرگرفتن transpose
- کنترل سینک آکوردها از Lyrics
- کنترل تغییر گام و کپی آکوردها
- همگام‌سازی highlight با پیام‌های playback

وابستگی‌های سرویس از طریق adapterهای popup، state آهنگ، bridge و توابع ترجمه
و transpose تزریق می‌شوند؛ بنابراین `core.js` دیگر منطق مستقیم این popup را
نگه نمی‌دارد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۴۱۸۱ → ۳۹۳۳ خط
کاهش خالص: ۲۴۸ خط
سرویس جدید: js/app/CoreChordLinePopupService.js
تست جدید: core-chord-line-popup-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۸۵ ورودی تست موفق
lint: ۱۵۹ فایل موفق
quality:diff-check: موفق
quality:architecture: موفق
quality:legacy-deps: موفق
```

بزرگ‌ترین بلوک باقی‌مانده برای مرحلهٔ بعد `syncLyricPopup` با حدود ۳۰۱ خط است.

## مرحلهٔ ۳۲: استخراج Player View Settings

در این مرحله مسئولیت‌های تنظیمات پنجرهٔ Player View از `syncLyricPopup` خارج شد و
به سرویس مستقل `CorePlayerViewSettingsService` منتقل شد. سرویس جدید مدیریت موارد
زیر را در یک مرز قابل‌آزمون انجام می‌دهد:

- بارگذاری و ذخیرهٔ تنظیمات فونت، رنگ، اندازه، ضخامت و نسبت اندازه‌ها
- اتصال کنترل‌های پنجره و میانبرهای Wheel
- اعمال تنظیمات به متن و آکوردهای Popup
- همگام‌سازی highlight خطوط با playhead
- ریسایز نوار آکورد آینه‌ای و fallback رندر آکورد

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۹۳۳ → ۳۷۰۰ خط
کاهش خالص: ۲۳۳ خط
سرویس جدید: js/app/CorePlayerViewSettingsService.js
تست جدید: js/tests/core-player-view-settings-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۸۶ ورودی تست موفق
npm run lint: ۱۶۰ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق
npm run quality:legacy-deps: موفق
```

بزرگ‌ترین بخش بعدی برای استخراج، بدنهٔ اصلی `syncLyricPopup` و سپس rendererهای
نمایش و setlist است.

## مرحلهٔ ۳۳: استخراج Player View Chord Renderer

موتور مستقل جای‌گذاری، رندر و پاک‌سازی آکوردهای پنجرهٔ Player View از بدنهٔ
`syncLyricPopup` خارج شد و به `CorePlayerViewChordRendererService` منتقل شد.
این سرویس اسکریپت داخلی Popup را می‌سازد و APIهای رندر، زمان‌بندی رندر، cache
المان‌ها و connectorهای عمودی را در همان محیط پنجره حفظ می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۷۰۰ → ۳۵۵۷ خط
کاهش خالص: ۱۴۳ خط
تابع syncLyricPopup: ۴۴۹ → ۳۰۶ خط
سرویس جدید: js/app/CorePlayerViewChordRendererService.js
تست جدید: js/tests/core-player-view-chord-renderer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۸۷ ورودی تست موفق
npm run lint: ۱۶۱ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق
npm run quality:legacy-deps: موفق
```

استخراج بعدی از بخش rebuild متن و ساختار Popup در همان تابع بزرگ انجام می‌شود.

## مرحلهٔ ۳۴: استخراج Player View Popup Sync

مسیر به‌روزرسانی Popup موجود از مسیر rebuild کامل جدا شد و به
`CorePlayerViewPopupSyncService` منتقل شد. این سرویس تغییر متن و ساختار خطوط،
به‌روزرسانی دادهٔ آکورد، پاک‌سازی cache هنگام تغییر ساختار، fallback رندر،
اعمال تنظیمات ذخیره‌شده و force-reflow را انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۵۵۷ → ۳۴۳۹ خط
کاهش خالص: ۱۱۸ خط
تابع syncLyricPopup: ۳۰۶ → ۱۶۸ خط
سرویس جدید: js/app/CorePlayerViewPopupSyncService.js
تست جدید: js/tests/core-player-view-popup-sync-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۸۸ ورودی تست موفق
npm run lint: ۱۶۲ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق
npm run quality:legacy-deps: موفق
```

در مرحلهٔ بعد، rebuild کامل Player View و سپس `renderPerfUI` به‌ترتیب اندازه
بررسی و استخراج می‌شوند.

## مرحلهٔ ۳۵: استخراج Player View Popup Builder

مسیر ساخت بدنهٔ Popup، قراردادن خطوط lyrics، ایجاد نوار chord mirror، اتصال
renderer آکورد و مقداردهی اولیهٔ تنظیمات به `CorePlayerViewPopupBuilderService`
منتقل شد. مسیر قدیمی برای حفظ رفتار CSS موجود فعلاً به‌صورت سازگاری باقی مانده
و در مرحلهٔ پاک‌سازی بعدی حذف می‌شود.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۴۳۹ → ۳۴۲۲ خط
کاهش خالص: ۱۷ خط
تابع syncLyricPopup: ۱۶۸ → ۱۳۶ خط
سرویس جدید: js/app/CorePlayerViewPopupBuilderService.js
تست جدید: js/tests/core-player-view-popup-builder-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۸۹ ورودی تست موفق
npm run lint: ۱۶۳ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق
npm run quality:legacy-deps: موفق
```

گام بعدی، حذف CSS و ساختار تکراری باقی‌مانده از `syncLyricPopup` و سپس شروع
استخراج تابع ۱۶۷ خطی `renderPerfUI` است.

## مرحلهٔ ۳۶: استخراج Performance UI Renderer

بدنهٔ ۱۶۷ خطی `renderPerfUI` از `core.js` خارج شد و به سرویس مستقل
`CorePerformanceUiService` منتقل شد. سرویس جدید رندر اطلاعات آهنگ، setlist،
جابه‌جایی drag-and-drop، ناوبری بخش‌ها، نشان یادداشت و اسکرول آهنگ جاری را
مدیریت می‌کند. wrapper کوچک داخل `core.js` برای حفظ API و callbackهای قبلی
باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۴۲۲ → ۳۲۸۵ خط
کاهش خالص: ۱۳۷ خط
تابع renderPerfUI: ۱۶۷ خط → wrapper سازگاری
سرویس جدید: js/app/CorePerformanceUiService.js
تست جدید: js/tests/core-performance-ui-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۰ ورودی تست موفق
npm run lint: ۱۶۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

استخراج بعدی از `syncLyricPopup` و سپس توابع بزرگ باقی‌ماندهٔ Performance انجام
می‌شود.

## مرحلهٔ ۳۷: حذف ساختار تکراری Player View

پس از انتقال builder به سرویس مستقل، بدنهٔ `syncLyricPopup` هنوز یک قالب CSS
کامل و آماده‌سازی تکراری داشت که بلافاصله توسط builder دوباره ساخته می‌شد.
این مسیر تکراری حذف شد و تابع فقط snapshot را آماده کرده و به
`CorePlayerViewPopupBuilderService` تحویل می‌دهد؛ رفتار sync موجود و API popup
بدون تغییر باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۲۸۵ → ۳۱۷۹ خط
کاهش خالص: ۱۰۶ خط
تابع syncLyricPopup: ۱۳۶ → ۳۰ خط
```

اعتبارسنجی:

```text
npm test: هر ۱۹۰ ورودی تست موفق
npm run lint: ۱۶۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

گام بعدی، بررسی و استخراج بزرگ‌ترین rendererهای ارنجر و سپس
`syncLyricOnlyPopup` است.

## مرحلهٔ ۳۸: استخراج Singer View Popup

تابع ۱۱۶ خطی `syncLyricOnlyPopup` به سرویس مستقل
`CoreLyricOnlyPopupService` منتقل شد. سرویس جدید ساخت پنجرهٔ خواننده، اتصال
پیام‌های sync، هایلایت مستقیم بر اساس playhead، اسکرول خط جاری و cleanup
listener را مدیریت می‌کند و wrapper سازگاری در `core.js` باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۱۷۹ → ۳۰۸۴ خط
کاهش خالص: ۹۵ خط
تابع syncLyricOnlyPopup: ۱۱۶ خط → wrapper سرویس
سرویس جدید: js/app/CoreLyricOnlyPopupService.js
تست جدید: js/tests/core-lyric-only-popup-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۱ ورودی تست موفق
npm run lint: ۱۶۵ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین rendererهای باقی‌ماندهٔ ارنجر و توابع آماده‌سازی پخش
استخراج می‌شوند.

## مرحلهٔ ۳۹: استخراج Arranger Preparation

تابع آماده‌سازی آهنگ بعدی، شامل clone دادهٔ آهنگ، تکمیل styleهای پیش‌فرض،
preload صدا، تبدیل sectionهای قدیمی، ساخت playback boundary، بازیابی markerها،
محاسبهٔ peaks، اعمال transpose و retry خطا، به
`CoreArrangerPreparationService` منتقل شد. state ارنجر همچنان در `core.js`
مالکیت دارد و سرویس فقط از طریق accessorها با آن کار می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۰۸۴ → ۳۰۱۰ خط
کاهش خالص: ۷۴ خط
تابع prepareNextArrSong: ۱۰۸ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerPreparationService.js
تست جدید: js/tests/core-arranger-preparation-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۲ ورودی تست موفق
npm run lint: ۱۶۶ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در گام بعد، بزرگ‌ترین rendererهای باقی‌ماندهٔ مدیریت ارنجر بررسی و استخراج
می‌شوند.

## مرحلهٔ ۴۰: استخراج Arranger Manager Renderer

تابع ۱۰۰ خطی `renderArrangerManager` به
`CoreArrangerManagerRendererService` منتقل شد. سرویس مستقل header، toolbar،
حالت خالی، کارت‌های playlist، badgeهای crossfade/pause و actionهای edit،
export و delete را می‌سازد؛ state و عملیات ذخیره همچنان از `core.js` تزریق
می‌شوند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۳۰۱۰ → ۲۹۳۹ خط
کاهش خالص: ۷۱ خط
تابع renderArrangerManager: ۱۰۰ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerManagerRendererService.js
تست جدید: js/tests/core-arranger-manager-renderer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۳ ورودی تست موفق
npm run lint: ۱۶۷ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

گام بعدی، استخراج مسیر ۹۹ خطی `importArrangerFromFile` است.

## مرحلهٔ ۴۱: استخراج Arranger File Import

مسیر ۹۹ خطی `importArrangerFromFile` به
`CoreArrangerFileImportService` منتقل شد. سرویس جدید انتخاب فایل JSON،
اعتبارسنجی فرمت و نسخه، کنترل نام تکراری، اعتبارسنجی songIdها، واردکردن
آهنگ‌های همراه، ساخت playlist استاندارد و اتصال به renderer/editor را مدیریت
می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۹۳۹ → ۲۸۶۸ خط
کاهش خالص: ۷۱ خط
تابع importArrangerFromFile: ۹۹ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerFileImportService.js
تست جدید: js/tests/core-arranger-file-import-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۴ ورودی تست موفق
npm run lint: ۱۶۸ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، renderer ۶۲ خطی `renderArrSetlist` استخراج می‌شود.

## مرحلهٔ ۴۲: استخراج Arranger Setlist Renderer

تابع ۶۲ خطی `renderArrSetlist` به `CoreArrangerSetlistRendererService` منتقل
شد. سرویس جدید رندر setlist، فیلتر زنده، جابه‌جایی drag-and-drop، کنترل‌های
بالا/پایین، حذف، تغییر گام و اتصال یادداشت اجرا را مدیریت می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۸۶۸ → ۲۸۲۶ خط
کاهش خالص: ۴۲ خط
تابع renderArrSetlist: ۶۲ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerSetlistRendererService.js
تست جدید: js/tests/core-arranger-setlist-renderer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۵ ورودی تست موفق
npm run lint: ۱۶۹ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

بزرگ‌ترین تابع فعلی `renderClips` است و پس از آن preload پس‌زمینهٔ Performance
استخراج می‌شود.

## مرحلهٔ ۴۳: استخراج Clip Renderer

تابع ۵۹ خطی `renderClips` به `CoreClipRendererService` منتقل شد. سرویس جدید
پاک‌سازی و ساخت clipهای صوتی و chord، waveform، انتخاب clip، نمایش مسیر فایل،
double-click و pointer interaction و رندر sectionها را مدیریت می‌کند؛ wrapper
با همان امضای قبلی برای حفظ قراردادهای Timeline باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۸۲۶ → ۲۷۹۰ خط
کاهش خالص: ۳۶ خط
تابع renderClips: ۵۹ خط → wrapper سرویس
سرویس جدید: js/app/CoreClipRendererService.js
تست جدید: js/tests/core-clip-renderer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۶ ورودی تست موفق
npm run lint: ۱۷۰ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

گام بعدی، استخراج `_startBackgroundPreload` در بخش Performance است.

## مرحلهٔ ۴۴: استخراج Background Preload

تابع ۵۷ خطی `_startBackgroundPreload` به
`CoreArrangerBackgroundPreloadService` منتقل شد. سرویس جدید preload ترتیبی،
تشخیص clipهای صوتی، ردکردن آهنگ‌های بدون صدا، بررسی bufferهای ازقبل‌آماده،
ثبت وضعیت پردازش و logging خطا را انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۷۹۰ → ۲۷۵۹ خط
کاهش خالص: ۳۱ خط
تابع _startBackgroundPreload: ۵۷ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerBackgroundPreloadService.js
تست جدید: js/tests/core-arranger-background-preload-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۷ ورودی تست موفق
npm run lint: ۱۷۱ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `exportArranger` و سپس `arrCrossfadeSwap`
بررسی می‌شوند.

## مرحلهٔ ۴۵: استخراج Arranger File Export

تابع ۴۴ خطی `exportArranger` به
`CoreArrangerFileExportService` منتقل شد. سرویس جدید ساخت دادهٔ خروجی،
پاک‌سازی نام فایل، پشتیبانی از `showSaveFilePicker` و مسیر fallback دانلود
با Blob را مستقل از وضعیت اصلی arranger مدیریت می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۷۶۰ → ۲۷۳۳ خط
کاهش خالص: ۲۷ خط
تابع exportArranger: ۴۴ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerFileExportService.js
تست جدید: js/tests/core-arranger-file-export-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۸ ورودی تست موفق
npm run lint: ۱۷۲ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `arrCrossfadeSwap` بررسی می‌شود.

## مرحلهٔ ۴۶: استخراج Arranger Crossfade

تابع ۴۱ خطی `arrCrossfadeSwap` به
`CoreArrangerCrossfadeService` منتقل شد. منطق fade-out، توقف صدای قبلی،
hot-swap آهنگ بعدی، fade-in، زمان‌بندی overlap و آزادسازی وضعیت crossfade
در سرویس جدید متمرکز شد و wrapper سازگار در `core.js` باقی ماند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۷۳۳ → ۲۷۱۴ خط
کاهش خالص: ۱۹ خط
تابع arrCrossfadeSwap: ۴۱ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerCrossfadeService.js
تست جدید: js/tests/core-arranger-crossfade-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۱۹۹ ورودی تست موفق
npm run lint: ۱۷۳ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `bufferToWave` بررسی می‌شود.

## مرحلهٔ ۴۷: استخراج WAV Encoder

تابع ۴۴ خطی `bufferToWave` به
`CoreWavEncoderService` منتقل شد. سرویس جدید هدر استاندارد RIFF/WAVE،
فرمت PCM شانزده‌بیتی، نمونه‌برداری چندکاناله و تبدیل نمونه‌های صوتی به
دادهٔ قابل ذخیره را مستقل از `core.js` انجام می‌دهد و wrapper قدیمی برای
سازگاری باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۷۱۴ → ۲۶۸۹ خط
کاهش خالص: ۲۵ خط
تابع bufferToWave: ۴۴ خط → wrapper سرویس
سرویس جدید: js/app/CoreWavEncoderService.js
تست جدید: js/tests/core-wav-encoder-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۰ ورودی تست موفق
npm run lint: ۱۷۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `openArrEditor` بررسی می‌شود.

## مرحلهٔ ۴۸: استخراج Arranger Editor

تابع ۳۷ خطی `openArrEditor` به
`CoreArrangerEditorService` منتقل شد. سرویس جدید بازکردن ادیتور،
نمایش modal، همگام‌سازی نام پلی‌لیست و crossfade، وضعیت pause، رندر pool و
setlist و بازنشانی تب ادیتور را مدیریت می‌کند و wrapper سازگار در `core.js`
باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۸۹ → ۲۶۷۲ خط
کاهش خالص: ۱۷ خط
تابع openArrEditor: ۳۷ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerEditorService.js
تست جدید: js/tests/core-arranger-editor-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۱ ورودی تست موفق
npm run lint: ۱۷۵ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `sendCurrentSongToArranger` بررسی می‌شود.

## مرحلهٔ ۴۹: استخراج Arranger Song Transfer

تابع ۳۳ خطی `sendCurrentSongToArranger` به
`CoreArrangerSongTransferService` منتقل شد. سرویس جدید ذخیرهٔ آهنگ جاری،
ساخت یا انتخاب پلی‌لیست، جلوگیری از افزودن تکراری، ذخیرهٔ arranger، بازکردن
پنجره و مدیریت خطا را مستقل از `core.js` انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۷۲ → ۲۶۶۳ خط
کاهش خالص: ۹ خط
تابع sendCurrentSongToArranger: ۳۳ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerSongTransferService.js
تست جدید: js/tests/core-arranger-song-transfer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۲ ورودی تست موفق
npm run lint: ۱۷۶ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `renderArrSongsList` بررسی می‌شود.

## مرحلهٔ ۵۰: استخراج Arranger Songs Overview

تابع ۳۱ خطی `renderArrSongsList` به
`CoreArrangerSongsOverviewService` منتقل شد. سرویس جدید رندر فهرست خلاصهٔ
ترانه‌های arranger، نمایش خواننده/گام/ریتم، تغییر گام و یادداشت‌های اجرا را
مستقل از `core.js` انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۶۲ → ۲۶۴۹ خط
کاهش خالص: ۱۳ خط
تابع renderArrSongsList: ۳۱ خط → wrapper سرویس
سرویس جدید: js/app/CoreArrangerSongsOverviewService.js
تست جدید: js/tests/core-arranger-songs-overview-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۳ ورودی تست موفق
npm run lint: ۱۷۷ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌مانده `syncLyricPopup` بررسی می‌شود.

## مرحلهٔ ۵۱: استخراج Player View Popup Sync

هماهنگ‌کنندهٔ `syncLyricPopup` از `core.js` به
`CorePlayerViewPopupService` منتقل شد. سرویس جدید چرخهٔ popup را از طریق
دو سرویس موجودِ `CorePlayerViewPopupSyncService` و
`CorePlayerViewPopupBuilderService` هماهنگ می‌کند و state پنجره، وضعیت آهنگ،
ترجمه و transpose را از طریق وابستگی‌های تزریق‌شده دریافت می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۴۹ → ۲۶۴۲ خط
کاهش خالص: ۷ خط
تابع syncLyricPopup: ۳۰ خط → wrapper سرویس
سرویس جدید: js/app/CorePlayerViewPopupService.js
تست جدید: js/tests/core-player-view-popup-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۴ ورودی تست موفق
npm run lint: ۱۷۸ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین تابع باقی‌ماندهٔ `core.js` شناسایی و استخراج می‌شود.

## مرحلهٔ ۵۲: استخراج Audio Blob Save Scheduler

منطق debounce و صف‌بندی `scheduleAudioBlobSave` از `core.js` به
`CoreAudioBlobSaveSchedulerService` منتقل شد. سرویس جدید ذخیرهٔ Blobهای صوتی
را با تأخیر ۱۲۰۰ میلی‌ثانیه انجام می‌دهد، درخواست‌های پیاپی را debounce
می‌کند، و اگر ذخیره‌ای در حال اجرا باشد یک اجرای بعدی را صف می‌گذارد؛
وابستگی به song state، storage service، timer و logger از بیرون تزریق می‌شود.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۴۲ → ۲۶۲۶ خط
کاهش خالص: ۱۶ خط
تابع scheduleAudioBlobSave: ۲۸ خط → wrapper سرویس
سرویس جدید: js/app/CoreAudioBlobSaveSchedulerService.js
تست جدید: js/tests/core-audio-blob-save-scheduler-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۵ ورودی تست موفق
npm run lint: ۱۷۹ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین واحد مستقل بعدی در `core.js` شناسایی می‌شود.

## مرحلهٔ ۵۳: استخراج Arranger Modal

منطق باز و بسته‌کردن modal arranger، مدیریت فوکوس و Escape، و drag handle
به `CoreArrangerModalService` منتقل شد. سرویس جدید state arranger را مالک
نمی‌شود و فقط از طریق accessorها، رندر manager، بازکردن editor و pointer-drag
را به runtime اصلی متصل می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۶۲۶ → ۲۵۸۵ خط
کاهش خالص: ۴۱ خط
واحدهای modal arranger: wrapper سرویس
سرویس جدید: js/app/CoreArrangerModalService.js
تست جدید: js/tests/core-arranger-modal-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۶ ورودی تست موفق
npm run lint: ۱۸۰ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین واحد مستقل باقی‌ماندهٔ `core.js` شناسایی می‌شود.

## مرحلهٔ ۷۵: استخراج workflow اصلی Auto Import

بزرگ‌ترین تابع باقی‌ماندهٔ `editor.js`، یعنی `startAutoImport` با ۲۰۲ خط،
به `js/editor/EditorAutoImportWorkflowService.js` منتقل شد. این سرویس
شناسایی تعداد ترانه‌ها، دریافت چند هنرمند، حذف نتایج تکراری، به‌روزرسانی
وضعیت، ذخیرهٔ اختیاری در آرشیو و گزارش نهایی را مالک می‌شود. در نتیجه
`editor.js` فقط dependency injection و wrapper فرمان را نگه می‌دارد.

رندر نتیجه‌ها نیز در مرز سرویس escape می‌شود تا نام هنرمند، عنوان، خطا و
کلید ترانه مستقیماً وارد HTML نشوند.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 3719 → 3543 خط
کاهش خالص: 175 خط
تابع منتقل‌شده: startAutoImport، ۲۰۲ خط
مالک جدید: js/editor/EditorAutoImportWorkflowService.js
```

اعتبارسنجی:

```text
تست EditorAutoImportWorkflowService: موفق
تست قرارداد runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۲۵ ورودی تست موفق
npm run lint: ۱۹۹ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با همان هشدار قبلی app.js
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یک workflow حجیم و چندفازی را از فایل اصلی خارج کرد و مسیر
Auto Import را برای حذف کامل وابستگی‌های legacy آماده‌تر کرد. اولویت
مرحلهٔ بعد نیز با بزرگ‌ترین بلوک منسجم باقی‌مانده است.

## مرحلهٔ ۷۶: استخراج بارگذاری آهنگ arranger

تابع `loadArrSong` با حدود ۱۰۴ خط منطق چندمرحله‌ای از `editor.js` خارج شد و
در `js/editor/EditorArrangerSongLoadService.js` قرار گرفت. سرویس جدید انتخاب
آهنگ از setlist، انتقال state آهنگ، بازیابی صوت، اعمال مرز پخش arranger،
پاک‌سازی state آماده‌سازی، رندر و همگام‌سازی popupها و زمان‌بندی آماده‌سازی
آهنگ بعدی را مالک می‌شود.

برای جلوگیری از بازگشت وابستگی‌های legacy، state عملکرد arranger با
`getPerformanceState` و `updatePerformanceState` تزریق شده و سرویس مستقیماً
به متغیرهای قدیمی دسترسی ندارد.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 3543 → 3517 خط
کاهش خالص: 26 خط
تابع منتقل‌شده: loadArrSong، حدود ۱۰۴ خط
مالک جدید: js/editor/EditorArrangerSongLoadService.js
```

اعتبارسنجی:

```text
تست EditorArrangerSongLoadService: موفق
تست قرارداد runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۲۶ ورودی تست موفق
npm run lint: ۲۰۰ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با همان هشدار قبلی app.js
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله مرز arranger را از متغیرهای legacy جدا کرد و ادامهٔ حذف منطق
متمرکز `editor.js` را کم‌ریسک‌تر کرد. اولویت مرحلهٔ بعد همچنان با بزرگ‌ترین
بلوک منسجم باقی‌مانده است.

## مرحلهٔ ۷۷: استخراج پشتیبان کامل پلی‌لیست‌ها

دو فرمان حجیم `exportAllPlaylistsToFile` و
`importAllPlaylistsFromFile` از `editor.js` خارج شدند و در
`js/editor/EditorPlaylistBackupService.js` قرار گرفتند. سرویس جدید ساخت
دادهٔ backup، خروجی با File Picker یا دانلود fallback، اعتبارسنجی فرمت و
نسخه، تشخیص نام‌های تکراری، ورود songهای موجود در backup، ساخت playlistهای
جدید و refresh آرشیو را مالک می‌شود.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 3517 → 3404 خط
کاهش خالص: 113 خط
بلوک منتقل‌شده: export/import کامل پلی‌لیست‌ها، حدود ۱۴۳ خط
مالک جدید: js/editor/EditorPlaylistBackupService.js
```

اعتبارسنجی:

```text
تست EditorPlaylistBackupService: موفق
تست قرارداد runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۲۷ ورودی تست موفق
npm run lint: ۲۰۱ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با همان هشدار قبلی app.js
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله دو فرمان فایل‌محور حجیم را از `editor.js` جدا کرد و مرز legacy
در مدیریت setlist را به یک سرویس قابل تست منتقل کرد. اولویت مرحلهٔ بعد
همچنان با بزرگ‌ترین منطق باقی‌مانده است، نه wrapperهای چندخطی.

## مرحلهٔ ۷۸: استخراج drag/dock نوار ابزار

بلوک حجیم drag، جابه‌جایی، snap/dock و منوی راست‌کلیک نوار ابزار از
`editor.js` خارج شد و به `js/editor/EditorToolbarDockService.js` منتقل شد.
سرویس جدید با dependencyهای تزریقی کار می‌کند، منطق DOM و viewport را مالک
است و global فرمان legacy برای این بخش را دوباره ایجاد نمی‌کند. در
`editor.js` فقط ساخت lazy سرویس، اتصال آن و wrapper داخلی فرمان باقی مانده
است.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۳۴۰۴ → ۳۲۴۹ خط
کاهش خالص: ۱۵۵ خط
بلوک منتقل‌شده: drag/dock/context menu نوار ابزار، حدود ۱۷۸ خط
مالک جدید: js/editor/EditorToolbarDockService.js
تست جدید: js/tests/editor-toolbar-dock-service.test.js
```

اعتبارسنجی:

```text
تست EditorToolbarDockService: موفق
تست قرارداد runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۲۸ ورودی تست موفق
npm run lint: ۲۰۲ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ editor.js زیر سقف است و هشدار قبلی app.js باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یک بلوک بزرگ و کاملاً تعاملی را از فایل متمرکز خارج کرد و یک
مرز مدرن، مستقل و قابل تست برای نوار ابزار ساخت. ادامهٔ کار با بزرگ‌ترین
تابع‌های باقی‌مانده انجام می‌شود تا مسیرهای legacy به‌صورت مرحله‌ای حذف
شوند.

## مرحلهٔ ۷۹: استخراج hot-swap آهنگ arranger

منطق حجیم و چندمسیرهٔ `hotSwapToNextSong` از `editor.js` خارج شد و به
`js/editor/EditorArrangerHotSwapService.js` منتقل شد. سرویس جدید state
آماده‌شده را مصرف می‌کند، تعویض امن song و audio را به
`EditorSongTransitionService` می‌سپارد، مرز پخش را اعمال می‌کند، recording
را reset می‌کند، رندر و ذخیره را هماهنگ می‌سازد و pause mode و آماده‌سازی
آهنگ بعدی را مدیریت می‌کند. تمام state و callbackها تزریق شده‌اند و در
`editor.js` فقط wiring و wrapper فرمان باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۳۲۴۹ → ۳۲۲۶ خط
کاهش خالص: ۲۳ خط
تابع منتقل‌شده: hotSwapToNextSong، ۸۴ خط
مالک جدید: js/editor/EditorArrangerHotSwapService.js
تست جدید: js/tests/editor-arranger-hot-swap-service.test.js
```

اعتبارسنجی:

```text
تست EditorArrangerHotSwapService: موفق
تست قرارداد arranger playback و runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۲۹ ورودی تست موفق
npm run lint: ۲۰۳ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ editor.js زیر سقف است و هشدار قبلی app.js باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یک فرمان واقعی و حجیم از مسیر arranger را از فایل متمرکز جدا
کرد و orchestration آن را به مرز قابل تست و بدون وابستگی مستقیم به globalها
منتقل کرد. اولویت مرحلهٔ بعد نیز با بزرگ‌ترین فرمان واقعی باقی‌مانده است.

## مرحلهٔ ۸۰: استخراج workflow خروجی کامل پروژه

فرمان واقعی `edExportProjectFull` از `editor.js` خارج شد و به
`js/editor/EditorProjectExportWorkflowService.js` منتقل شد. سرویس جدید
ساخت bundle را از سرویس export، ذخیرهٔ Native را از سرویس فایل و ذخیرهٔ
مرورگر را از route service هماهنگ می‌کند؛ پیام progress، حجم خروجی،
تعداد فایل‌های کپی/لینک‌شده، لغو و خطا نیز در همین مرز مدیریت می‌شوند.
در `editor.js` فقط ساخت lazy سرویس و wrapper فرمان باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۳۲۲۶ → ۳۱۸۹ خط
کاهش خالص: ۳۷ خط
تابع منتقل‌شده: edExportProjectFull، ۶۵ خط
مالک جدید: js/editor/EditorProjectExportWorkflowService.js
تست جدید: js/tests/editor-project-export-workflow-service.test.js
```

اعتبارسنجی:

```text
تست EditorProjectExportWorkflowService: موفق
تست قرارداد export/native-save و runtime/dependency/load-order/encoding/legacy: موفق
npm test: هر ۲۳۰ ورودی تست موفق
npm run lint: ۲۰۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ editor.js زیر سقف است و هشدار قبلی app.js باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله orchestration خروجی پروژه را از فایل متمرکز جدا کرد و مالکیت
ساخت داده و نوشتن فایل را از workflow UI تفکیک کرد. مرحلهٔ بعد با فرمان
واقعی حجیم `autoRetryFailed` ادامه پیدا می‌کند.

## مرحلهٔ ۸۱: استخراج retry خودکار Auto Import

فرمان واقعی `autoRetryFailed` از `editor.js` خارج شد و به
`js/editor/EditorAutoImportRetryService.js` منتقل شد. سرویس جدید گروه‌بندی
ترانه‌های ناموفق، retry per-artist، به‌روزرسانی progress و status، ثبت
ترانه‌های بازیابی‌شده در state و archive، و نگه‌داشتن مواردی را که هنوز
ناموفق‌اند مالک می‌شود. در `editor.js` فقط ساخت lazy سرویس و wrapper فرمان
باقی مانده است.

در همین انتقال یک خطای قدیمی نیز اصلاح شد: شرط
`!rawText?.includes('')` عملاً متن معتبر را بازیابی نمی‌کرد؛ اکنون بازیابی
بر اساس وجود واقعی `rawText` انجام می‌شود و فقط نتایج قابل ذخیره از فهرست
ناموفق حذف می‌شوند.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۳۱۸۹ → ۳۱۵۶ خط
کاهش خالص: ۳۳ خط
تابع منتقل‌شده: autoRetryFailed، حدود ۶۳ خط
مالک جدید: js/editor/EditorAutoImportRetryService.js
تست جدید: js/tests/editor-auto-import-retry-service.test.js
```

اعتبارسنجی:

```text
تست EditorAutoImportRetryService و قرارداد markup safety: موفق
npm test: هر ۲۳۱ ورودی تست موفق
npm run lint: ۲۰۵ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ editor.js زیر سقف است و هشدار قبلی app.js باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یکی از فرمان‌های واقعی باقی‌مانده را از فایل متمرکز جدا کرد و
مسیر retry را بدون بازگرداندن globalهای legacy قابل تست کرد. اولویت مرحلهٔ
بعد نیز با بزرگ‌ترین تابع واقعی باقی‌مانده است.

## مرحلهٔ ۷۴: استخراج ذخیرهٔ فایل‌های Auto Import

بزرگ‌ترین تابع باقی‌ماندهٔ `editor.js`، یعنی `autoImportDoSave` با ۴۳۴ خط،
به `js/editor/EditorAutoImportFileSaveService.js` منتقل شد. این سرویس هر دو
مسیر ذخیرهٔ Native File System و ذخیرهٔ سروری، پاک‌سازی نام فایل و پوشه،
جلوگیری از نام‌های تکراری، گزارش per-artist و مدیریت خطاها را مالک می‌شود.
در `editor.js` فقط اتصال dependencyها و wrapper فرمان باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4136 → 3719 خط
کاهش خالص: 417 خط
تابع منتقل‌شده: autoImportDoSave، ۴۳۴ خط
مالک جدید: js/editor/EditorAutoImportFileSaveService.js
```

اعتبارسنجی:

```text
تست EditorAutoImportFileSaveService: موفق
تست‌های قرارداد runtime/load-order/encoding/legacy: موفق
npm test: هر ۲۲۴ ورودی تست موفق
npm run lint: ۱۹۸ فایل موفق
npm run quality:architecture: موفق؛ هشدار قبلی app.js همچنان باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
npm run quality:diff-check: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یک تابع حجیم و چندمسیره را از فایل اصلی خارج کرد و مرز legacy در
روند Auto Import را کوچک‌تر و قابل تست کرد. اولویت مرحلهٔ بعد نیز با بزرگ‌ترین
بلوک منسجم باقی‌مانده است.

## مرحلهٔ ۷۳: استخراج ابزار رنگ و brush تایم‌لاین

بلوک بزرگ ابزار رنگ از `editor.js` خارج شد و در
`js/editor/EditorColorToolService.js` قرار گرفت. این سرویس state ابزار،
پالت سریع، انتخاب رنگ، paint/eyedropper برای متن، آکورد، کلیپ و بخش و drag
brush روی timeline را مالک می‌شود. `editor.js` فقط dependency injection و
API اتصال به سرویس را نگه می‌دارد.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4569 → 4136 خط
کاهش خالص: 434 خط
بلوک منتقل‌شده: ۴۸۱ خط رفتار رنگ و brush
مالک جدید: js/editor/EditorColorToolService.js
```

اعتبارسنجی:

```text
تست EditorColorToolService: موفق
تست Timeline interaction contract: موفق
تست Script load order: موفق
تست Runtime dependency budget: موفق
npm test: هر ۲۲۳ ورودی تست موفق
npm run lint: ۱۹۷ فایل موفق
npm run quality:architecture: موفق؛ هشدار قبلی app.js همچنان باقی است
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
npm run quality:diff-check: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله یکی از بزرگ‌ترین بلوک‌های باقی‌ماندهٔ `editor.js` را به سرویس
مستقل تبدیل کرد و مسیر حذف کامل legacy را با کاهش واقعی حجم فایل ادامه داد.
در گام بعدی اولویت با یک بلوک حجیم دیگر از `editor.js` است، نه bridgeهای
چندخطی.

## مرحلهٔ ۷۲: حذف globalهای layout تایم‌لاین

ساخت و نگهداری `TimelineScrollbarsService` و `TimelinePanelLayoutService`
از `editor.js` خارج شد و به `CorePanelLayoutService` منتقل شد. همچنین
`projectPanelLayout` و `songPropertiesPanelLayout` دیگر روی global scope منتشر
نمی‌شوند و داخل map خصوصی سرویس نگهداری می‌شوند. بنابراین docking، resizing،
همگام‌سازی scroll و toggle پنل‌ها از API داخلی سرویس انجام می‌شوند.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4588 → 4569 خط
کاهش خالص: 19 خط
globalهای حذف‌شده: timelineScrollbars، timelinePanelLayout،
projectPanelLayout، songPropertiesPanelLayout
مالک جدید: js/app/CorePanelLayoutService.js
core.js: 2556 خط
```

اعتبارسنجی:

```text
تست CorePanelLayoutService: موفق
تست Timeline interaction contract: موفق
تست Script load order: موفق
تست Runtime dependency budget: موفق
بررسی syntax فایل‌های تغییر‌یافته: موفق
```

این مرحله انتشار مستقیم state و handleهای layout را از بین برد و مالکیت آن‌ها
را داخل سرویس رسمی نگه داشت. مسیر حذف کامل الگوی legacy ادامه دارد و در گام
بعدی انتشارهای باقی‌ماندهٔ runtime در `editor.js` ممیزی و کوچک‌تر می‌شوند.

## مرحلهٔ ۷۱: حذف EditorLegacySongBridge

پل عمومی `EditorLegacySongBridge` از `editor.js` حذف شد و
`EditorSongRuntimeService` نیز دیگر هیچ fallback یا dependency روی این پل
ندارد. مالکیت آهنگ جاری اکنون مستقیماً از مسیر رسمی
`EditorRuntimeAdapter/EdCurAdapter` انجام می‌شود و سرویس runtime فقط همین
قرارداد را expose می‌کند.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4592 → 4588 خط
کاهش خالص: 4 خط
حذف‌شده: EditorLegacySongBridge و fallbackهای getLegacySong/setLegacySong
EditorSongRuntimeService: مستقیم بر پایهٔ EditorRuntimeAdapter/EdCurAdapter
core.js: بدون تغییر، 2555 خط
```

اعتبارسنجی:

```text
npm test: هر 222 ورودی تست موفق
npm run lint: 196 فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با 191 خط زیر سقف 200 و با هشدار target
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

با این مرحله، یکی از آخرین پل‌های عمومی state آهنگ حذف شد. گام بعدی حذف
انتشار مستقیم runtime تایم‌لاین و تبدیل آن به API رسمی سرویس‌هاست.

## مرحلهٔ ۷۰: حذف side-channel موقت importParsed

bridge موقت `_importParsed` از `editor.js` و `ArchiveModule.js` حذف شد.
مسیر import پروژه دیگر state را از طریق متغیر مشترک منتقل نمی‌کند و دادهٔ
parsed را مستقیماً به شکل `applyImportChords(parsed)` به سرویس import می‌دهد.
در نتیجه، state داخلی `EditorChordImportService` خصوصی باقی می‌ماند و مسیر
import پروژه نیز یک API صریح و قابل‌آزمون دارد.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4598 → 4592 خط
کاهش خالص: 6 خط
حذف‌شده: _importParsed، getImportParsed خارجی، setImportParsedExternal
تغییر API: ArchiveProjectFileImportService → applyImportChords(parsed)
```

اعتبارسنجی:

```text
npm test: هر 222 ورودی تست موفق
npm run lint: 196 فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با 191 خط زیر سقف 200 و با هشدار target
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

این مرحله یک side-channel legacy دیگر را حذف کرد و مسیر import پروژه را به
قرارداد سرویس‌محور منتقل کرد. گام بعدی، جدا کردن bridge آهنگ جاری و سپس
حذف انتشار globalهای runtime تایم‌لاین است.

## مرحلهٔ ۶۹: حذف state عمومی Auto Import

state موقت Auto Import از bindingهای عمومی `window._ai*` و
`window._autoImportDirHandle` خارج شد و به سرویس خصوصی
`EditorAutoImportStateService` منتقل شد. `editor.js` اکنون فقط از API محدود
سرویس برای نتایج، آمار، موارد ناموفق و پوشهٔ ذخیره استفاده می‌کند؛ بنابراین
این state دیگر از طریق global قابل دست‌کاری نیست. حفظ directory handle هنگام
reset نیز برای سازگاری با رفتار قبلی عمداً انجام می‌شود.

نتیجهٔ اندازه‌گیری:

```text
editor.js: 4595 → 4598 خط
تغییر خالص: 3 خط افزایش (به‌دلیل wiring سرویس؛ state و منطق global حذف شد)
stateهای حذف‌شده: _aiResults، _aiArtistMap، _aiStats، _aiFailedSongs،
_aiFailedFiles، _autoImportDirHandle
سرویس جدید: js/editor/EditorAutoImportStateService.js
تست جدید: js/tests/editor-auto-import-state-service.test.js
core.js: بدون تغییر، 2555 خط
```

اعتبارسنجی:

```text
npm test: هر 222 ورودی تست موفق
npm run lint: 196 فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با 191 خط زیر سقف 200 و با هشدار target
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

این مرحله یکی از مرزهای اصلی حذف کامل legacy را جمع کرد. در گام‌های بعدی
wrapperها، bridgeها و global bindingهای باقی‌ماندهٔ `editor.js` شناسایی و به
APIهای رسمی منتقل می‌شوند تا در پایان سیستم legacy به‌طور کامل حذف شود.

## مرحلهٔ ۶۸: استخراج MIDI Input و Evaluation

منطق دریافت پیام MIDI، ارزیابی نت‌ها، تشخیص و نمایش آکورد، هایلایت
کلاویه، ضبط آکورد روی timeline و همگام‌سازی MIDI از `editor.js` به
`EditorMidiInputService` منتقل شد. سرویس جدید state و عملیات runtime را
فقط از طریق accessor و callback دریافت می‌کند و هیچ وابستگی مستقیمی به
`edCur`، `DAW` یا `PERF` ندارد. wrapperهای action موجود برای حفظ سازگاری
فعلاً باقی مانده‌اند و در مرحلهٔ حذف نهایی legacy جمع می‌شوند.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۴۶۶۶ → ۴۵۹۵ خط
کاهش خالص: ۷۱ خط
منطق استخراج‌شده: handleMIDIMessage، evaluateMidiInput، highlightPianoKey،
identifyChord و toggleMIDISync
سرویس جدید: js/editor/EditorMidiInputService.js
تست جدید: js/tests/editor-midi-input-service.test.js
core.js: بدون تغییر، ۲۵۵۵ خط
```

اعتبارسنجی:

```text
npm test: هر ۲۲۱ ورودی تست موفق
npm run lint: ۱۹۵ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۹۰ خط زیر سقف ۲۰۰ و با هشدار target
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

این مرحله یک مرز مستقل دیگر برای حذف کامل legacy ایجاد کرد؛ در گام بعدی
مصرف‌کننده‌های باقی‌ماندهٔ wrapperها و bindingهای قدیمی شناسایی و به
runtime APIهای رسمی منتقل می‌شوند.

## مرحلهٔ ۶۷: استخراج Chord Import

منطق import آکورد از متن و URL از `editor.js` به
`EditorChordImportService` منتقل شد. سرویس جدید بازکردن/بستن modal،
بارگذاری نتیجهٔ auto-import، دریافت صفحات Laminor و Akord، parser عمومی،
preview و اعمال نتیجه روی song جاری را مدیریت می‌کند. برای سازگاری با
`ArchiveProjectFileImportService`، binding قدیمی `_importParsed` به‌صورت
bridge باقی ماند و state سرویس از طریق getter/setter تزریق‌شده همگام می‌شود.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۴۸۶۷ → ۴۶۶۶ خط
کاهش خالص: ۲۰۱ خط
توابع استخراج‌شده: openImportChordModal، closeImportChordModal،
loadAutoImportSong، fetchFromUrl، parseChordPage، showImportPreview،
applyImportChords
سرویس جدید: js/editor/EditorChordImportService.js
تست جدید: js/tests/editor-chord-import-service.test.js
core.js: بدون تغییر، ۲۵۵۵ خط
```

اعتبارسنجی:

```text
npm test: هر ۲۲۰ ورودی تست موفق
npm run lint: ۱۹۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۹ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

در ادامه، واحد مستقل بعدی از `editor.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۶: استخراج Timeline Chord Editor

منطق کامل modal آکورد تایم‌لاین از `editor.js` به
`EditorTimelineChordEditorService` منتقل شد. این سرویس ساخت ستون‌های
انتخاب آکورد، پیانوی دو اکتاو، preview، بازکردن/بستن modal، ثبت/حذف و
ایجاد یا ویرایش clip را در اختیار می‌گیرد و state و عملیات render/save را
از طریق callbackهای تزریق‌شده دریافت می‌کند. dispatch مودال برای دو حالت
timeline و ویرایشگر chord نیز حفظ شد.

نتیجهٔ اندازه‌گیری:

```text
editor.js: ۴۹۵۱ → ۴۸۶۷ خط
کاهش خالص: ۸۴ خط
توابع استخراج‌شده: buildChordEditor، buildPiano، updateChordPreview،
openChordEditor، closeChordEditor، chordModalConfirm، chordModalDelete،
placeChordOnTimeline
سرویس جدید: js/editor/EditorTimelineChordEditorService.js
تست جدید: js/tests/editor-timeline-chord-editor-service.test.js
core.js: بدون تغییر، ۲۵۵۵ خط
```

اعتبارسنجی:

```text
npm test: هر ۲۱۹ ورودی تست موفق
npm run lint: ۱۹۳ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۸ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:core-refactor: موفق
npm run quality:legacy-deps: موفق
```

در ادامه، واحد مستقل بعدی از `editor.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۵: استخراج Sequential Chord Remap

remap نقاط chord ترتیبی بعد از ویرایش متن به
`CoreSequentialChordRemapService` منتقل شد. سرویس جدید از
`LyricPositionMapper` برای به‌روزرسانی in-place لنگرها استفاده می‌کند،
نقاط نامعتبر را حذف می‌کند و فقط در حالت فعال sequential، state runtime را
با نقاط معتبر sync می‌کند؛ state song همچنان از طریق accessor تزریق می‌شود.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۴۸ → ۲۵۵۵ خط
تغییر خالص: ۷ خط افزایش به‌دلیل wiring تزریق‌شده؛ منطق remap از core خارج شد
تابع edRemapSeqPoints: wrapper سرویس
سرویس جدید: js/app/CoreSequentialChordRemapService.js
تست جدید: js/tests/core-sequential-chord-remap-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۸ ورودی تست موفق
npm run lint: ۱۹۲ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۷ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۴: استخراج Timeline Chord Editor Bridge

منطق بازکردن modal ویرایش chord از روی clip تایم‌لاین به
`CoreTimelineChordEditorBridgeService` منتقل شد. bridge جدید نوع clip، وجود
handler، و debounce کوتاه ۱۲۰ میلی‌ثانیه‌ای را کنترل می‌کند و همان callback
را به clip renderer و clip interaction می‌دهد تا از رفتار duplicate-open
جلوگیری شود.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۴۲ → ۲۵۴۸ خط
تغییر خالص: ۶ خط افزایش به‌دلیل wiring تزریق‌شده؛ منطق debounce از core خارج شد
تابع openTimelineChordEditor: wrapper سرویس
سرویس جدید: js/app/CoreTimelineChordEditorBridgeService.js
تست جدید: js/tests/core-timeline-chord-editor-bridge-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۷ ورودی تست موفق
npm run lint: ۱۹۱ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۶ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۳: استخراج Arranger Editor Actions

actionهای shell ویرایش arranger به `CoreArrangerEditorActionsService` منتقل
شدند: جابه‌جایی بین tabهای editor و songs، بستن editor و export arranger
فعلی. سرویس جدید state اصلی را مالک نیست و از callbackهای render، save،
selection و export استفاده می‌کند؛ `CoreArrangerEditorService` نیز همچنان
open editor را از طریق callback `switchArrTab` انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۴۳ → ۲۵۴۲ خط
کاهش خالص: ۱ خط
توابع switchArrTab، closeArrEditor و exportCurrentArranger: wrapper سرویس
سرویس جدید: js/app/CoreArrangerEditorActionsService.js
تست جدید: js/tests/core-arranger-editor-actions-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۶ ورودی تست موفق
npm run lint: ۱۹۰ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۵ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۲: استخراج Arranger Marker Bridge

ساخت و lifecycle مربوط به `EditorArrangerMarkerControllerService` به
`CoreArrangerMarkerBridgeService` منتقل شد. bridge جدید dependencyهای marker،
geometry، drag و persistence را تزریق می‌کند، controller را می‌سازد و
`bindDrag` را یک‌بار اجرا می‌کند؛ APIهای marker همچنان از مسیر core و editor
در دسترس باقی می‌مانند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۵۷ → ۲۵۴۳ خط
کاهش خالص: ۱۴ خط
ساخت controller و wrapperهای marker: bridge سرویس
سرویس جدید: js/app/CoreArrangerMarkerBridgeService.js
تست جدید: js/tests/core-arranger-marker-bridge-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۵ ورودی تست موفق
npm run lint: ۱۸۹ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۴ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۱: استخراج Arranger Controls

کنترل‌های ویرایش arranger به `CoreArrangerControlsService` منتقل شدند:
تنظیم crossfade، تغییر حالت مکث بین آهنگ‌ها، transpose گروهی، پاک‌کردن
یادداشت‌های همهٔ آهنگ‌ها و filter کردن pool/setlist. سرویس جدید state اصلی
arranger را مالک نیست و فقط از accessorها و callbackهای persistence/render
استفاده می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۶۹ → ۲۵۵۷ خط
کاهش خالص: ۱۲ خط
توابع arrSetCrossfade، arrTogglePauseBetween، arrAutoTranspose،
arrClearNotes و arrFilterSongs: wrapper سرویس
سرویس جدید: js/app/CoreArrangerControlsService.js
تست جدید: js/tests/core-arranger-controls-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۴ ورودی تست موفق
npm run lint: ۱۸۸ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۳ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۶۰: استخراج Arranger Song Note

state و رفتار modal یادداشت آهنگ‌های Arranger به
`CoreArrangerSongNoteService` منتقل شد. سرویس جدید بازکردن overlay،
نمایش عنوان آهنگ و note فعلی، ذخیرهٔ متن، بستن modal و refresh setlist را
مدیریت می‌کند؛ collection اصلی arranger همچنان از طریق accessor در `core.js`
باقی می‌ماند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۶۵ → ۲۵۶۹ خط
تغییر خالص: ۴ خط افزایش به‌دلیل wiring تزریق‌شده؛ منطق modal از core خارج شد
توابع openArrSongNote، closeArrSongNote و saveArrSongNote: wrapper سرویس
سرویس جدید: js/app/CoreArrangerSongNoteService.js
تست جدید: js/tests/core-arranger-song-note-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۳ ورودی تست موفق
npm run lint: ۱۸۷ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۲ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۵۹: استخراج Timeline Section Bridge

ساخت lazy و wiring مربوط به `TimelineSectionRendererService` از `core.js`
به `CoreTimelineSectionBridgeService` منتقل شد. bridge جدید renderer را فقط
در اولین درخواست می‌سازد و callbackهای geometry، selection، drag و persistence
را دریافت می‌کند؛ منطق رندر section همچنان در سرویس تخصصی خودش باقی مانده است.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۶۵ → ۲۵۶۵ خط
کاهش خالص: ۰ خط؛ wiring جدید جایگزین wiring قبلی شد
تابع getTimelineSectionRendererService: wrapper سرویس
سرویس جدید: js/app/CoreTimelineSectionBridgeService.js
تست جدید: js/tests/core-timeline-section-bridge-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۲ ورودی تست موفق
npm run lint: ۱۸۶ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۱ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۵۸: استخراج Timeline Grid Bridge

پل ارتباطی grid تایم‌لاین از `core.js` به
`CoreTimelineGridService` منتقل شد. سرویس جدید `drawLaneGrid`،
`renderRuler` و چرخهٔ refresh مربوط به `handleTimingChange` را در اختیار
می‌گیرد؛ منطق رسم جزئیات همچنان متعلق به `TimelineGrid` باقی مانده و state
transport و timing از طریق accessor دریافت می‌شود.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۷۱ → ۲۵۶۵ خط
کاهش خالص: ۶ خط
توابع drawLaneGrid، renderRuler و handleTimingChange: wrapper سرویس
سرویس جدید: js/app/CoreTimelineGridService.js
تست جدید: js/tests/core-timeline-grid-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۱ ورودی تست موفق
npm run lint: ۱۸۵ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق؛ app.js با ۱۸۰ خط همچنان زیر سقف ۲۰۰ و با هشدار قبلی
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۵۷: استخراج History Bridge

اتصال `HistoryService` به runtime و editor state از `core.js` خارج و به
`CoreHistoryBridgeService` منتقل شد. سرویس جدید فقط wiring وابستگی‌ها را انجام
می‌دهد و مالک state و منطق editor نیست؛ `attachHistoryService` در `core.js`
اکنون یک wrapper باریک برای همین bridge است. قراردادهای script load order،
runtime dependency budget، encoding integrity و legacy runtime scripts نیز برای
سرویس جدید به‌روزرسانی شدند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۶۱ → ۲۵۷۱ خط
تغییر خالص: ۱۰ خط افزایش به‌دلیل wiring تزریق‌شده؛ منطق History از core خارج شد
تابع attachHistoryService: منطق داخلی → wrapper سرویس
سرویس جدید: js/app/CoreHistoryBridgeService.js
تست جدید: js/tests/core-history-bridge-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۱۰ ورودی تست موفق
npm run lint: ۱۸۴ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، یک واحد مستقل دیگر از `core.js` برای استخراج انتخاب می‌شود.

## مرحلهٔ ۵۶: استخراج Arranger Pool Renderer

رندر فهرست آهنگ‌های قابل‌افزودن به arranger به
`CoreArrangerPoolRendererService` منتقل شد. سرویس جدید filtering بر اساس
عنوان/خواننده/گام/ژانر، حالت خالی، ساخت کارت آهنگ و افزودن آهنگ به setlist را
مدیریت می‌کند و collection arranger را از طریق accessor دریافت می‌کند.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۶۲ → ۲۵۶۱ خط
کاهش خالص: ۱ خط
تابع renderArrPool: wrapper سرویس
سرویس جدید: js/app/CoreArrangerPoolRendererService.js
تست جدید: js/tests/core-arranger-pool-renderer-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۹ ورودی تست موفق
npm run lint: ۱۸۳ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین واحد مستقل باقی‌ماندهٔ `core.js` شناسایی می‌شود.

## مرحلهٔ ۵۵: استخراج Arranger Save

ذخیرهٔ arranger فعلی و debounce تغییر نام آن به
`CoreArrangerSaveService` منتقل شد. سرویس جدید اعتبارسنجی نام تکراری،
به‌روزرسانی نام/تاریخ/crossfade، ذخیره و refresh manager را انجام می‌دهد و
timer debounce را به‌صورت مستقل نگه می‌دارد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۸۰ → ۲۵۶۲ خط
کاهش خالص: ۱۸ خط
توابع saveCurrentArranger و saveCurrentArrangerDebounced: wrapper سرویس
سرویس جدید: js/app/CoreArrangerSaveService.js
تست جدید: js/tests/core-arranger-save-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۸ ورودی تست موفق
npm run lint: ۱۸۲ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین واحد مستقل باقی‌ماندهٔ `core.js` شناسایی می‌شود.

## مرحلهٔ ۵۴: استخراج Arranger Creation

منطق ساخت arranger جدید به `CoreArrangerCreationService` منتقل شد. سرویس
جدید prompt نام، تشخیص نام تکراری، recursion برای درخواست دوباره، ساخت
ساختار playlist، ثبت تاریخ‌ها، انتخاب arranger تازه و refresh کردن manager و
editor را بدون مالکیت collection اصلی انجام می‌دهد.

نتیجهٔ اندازه‌گیری:

```text
core.js: ۲۵۸۵ → ۲۵۸۰ خط
کاهش خالص: ۵ خط
تابع createNewArranger: wrapper سرویس
سرویس جدید: js/app/CoreArrangerCreationService.js
تست جدید: js/tests/core-arranger-creation-service.test.js
```

اعتبارسنجی:

```text
npm test: هر ۲۰۷ ورودی تست موفق
npm run lint: ۱۸۱ فایل موفق
npm run quality:diff-check: موفق
npm run quality:architecture: موفق با هشدار قبلی app.js
npm run quality:legacy-deps: موفق
```

در ادامه، بزرگ‌ترین واحد مستقل باقی‌ماندهٔ `core.js` شناسایی می‌شود.
